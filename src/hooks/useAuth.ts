import { useState,useEffect,useCallback,useRef } from 'react';
import { supabase,getProfileByAuthId,getProfileByEmail,linkProfileToAuth,createProfile,trackSession,endSession,createUserSession,deactivateUserSession,getStoredSessionId,updateSessionLastSeen } from '@/lib/supabase';
import type { Profile,Page } from '@/types';
interface AuthState{page:Page;profile:Profile|null;isLoading:boolean;error:string;kickedOut?:boolean}
async function wipeOnLogout(){try{const keys:string[]=[];for(let i=localStorage.length-1;i>=0;i--){const k=localStorage.key(i);if(k&&(k.includes('sb-')||k.includes('supabase')||k.startsWith('wh_')))keys.push(k)}keys.forEach(k=>localStorage.removeItem(k));sessionStorage.clear()}catch{}}
const ADMIN_ROLES=new Set(['creator','admin']);export const hasAdminAccess=(r:string)=>ADMIN_ROLES.has(r);export const isStaff=(r:string)=>r==='staff';export const canCreateListings=(r:string)=>['staff','admin','creator'].includes(r);export const isAdmin=(r:string)=>r==='admin';export const isCreator=(r:string)=>r==='creator';export const canSendAnnouncements=(r:string)=>isCreator(r)||isAdmin(r);export function getScope(r:string):'global'|'local'|null{return isCreator(r)||isAdmin(r)?'global':r==='staff'?'local':null}export const isGlobal=(r:string)=>getScope(r)==='global';export function canModifyRole(m:string,t:string){if(isCreator(m))return !isCreator(t);if(isAdmin(m))return !isCreator(t)&&!isAdmin(t);return false}export function validateRoleTransition(m:string,c:string,n:string):{allowed:boolean;reason?:string}{if(isCreator(c))return{allowed:false,reason:'Creator role cannot be modified'};if(isCreator(n))return{allowed:false,reason:'Creator role cannot be assigned'};if(c==='worker'||n==='worker')return{allowed:false,reason:'Worker role is managed through worker registration.'};if(c==='property_partner'||n==='property_partner')return{allowed:false,reason:'Property Partner role is managed through partner registration.'};if(isCreator(m)&&['user','staff','admin'].includes(c)&&['user','staff','admin'].includes(n))return{allowed:true};if(isAdmin(m)&&['user','staff'].includes(c)&&['user','staff'].includes(n))return{allowed:true};return{allowed:false,reason:'You cannot make that role change'}}
async function setting(key:string){try{const{data}=await supabase.from('platform_settings').select('value').eq('key',key).maybeSingle();return data?.value==null?null:String(data.value)}catch{return null}}async function maintenance(){const v=await setting('maintenance_mode');return v==='true'||v==='on'||v==='1'}async function registrationClosed(){const v=await setting('registration_open');return v==='false'||v==='closed'||v==='0'}
export function useAuth(){const[state,setState]=useState<AuthState>({page:'loading',profile:null,isLoading:true,error:'',kickedOut:false});const handlingLoginRef=useRef(false),weTriggeredSignOutRef=useRef(false),profileLoadRef=useRef<Promise<void>|null>(null);const determinePage=useCallback((p:Profile|null):Page=>{if(!p)return'login';if(!p.profile_complete&&p.role==='worker')return'worker_setup';if(!p.profile_complete)return'setup';if(p.role==='worker')return'worker_dashboard';if(isCreator(p.role))return'creator';if(p.role==='admin')return'admin';if(p.role==='staff')return'staff_dashboard';if(p.role==='property_partner')return'property_partner';return'dashboard'},[]);
const allowEntry=useCallback(async(p:Profile)=>{if(p.banned||p.suspended||p.deleted){weTriggeredSignOutRef.current=true;await supabase.auth.signOut({scope:'local'});await wipeOnLogout();setState({page:'login',profile:null,isLoading:false,error:p.banned?'Your account has been permanently banned. Contact support for assistance.':p.suspended?'Your account has been suspended. Contact support for assistance.':'This account has been deleted.'});return false}if(!isCreator(p.role)&&!(p as any).maintenance_exempt&&await maintenance()){setState({page:'login',profile:null,isLoading:false,error:'WeHouse is currently under maintenance. Please check back later.'});return false}return true},[]);
const loadProfile=useCallback(async(authId:string,{preserveOnFailure=false}:{preserveOnFailure?:boolean}={})=>{if(profileLoadRef.current)return profileLoadRef.current;profileLoadRef.current=(async()=>{try{const{data:userData}=await supabase.auth.getUser();const email=userData?.user?.email;const{profile,error}=await getProfileByAuthId(authId,email);if(error||!profile){if(!preserveOnFailure)setState(s=>({...s,isLoading:false,error:'We could not load your account. Check your connection and try again.'}));return}if(await allowEntry(profile))setState({profile,page:determinePage(profile),isLoading:false,error:'',kickedOut:false})}catch{if(!preserveOnFailure)setState(s=>({...s,isLoading:false,error:'Connection interrupted. Retrying your session…'}))}finally{profileLoadRef.current=null}})();return profileLoadRef.current},[allowEntry,determinePage]);
useEffect(()=>{let alive=true;async function restore(){setState(s=>({...s,isLoading:true}));for(let attempt=0;attempt<3&&alive;attempt++){try{const{data,error}=await supabase.auth.getSession();if(error)throw error;if(data.session?.user){await loadProfile(data.session.user.id,{preserveOnFailure:attempt<2});return}if(attempt===0){await new Promise(r=>setTimeout(r,700));continue}setState({page:'login',profile:null,isLoading:false,error:'',kickedOut:false});return}catch{await new Promise(r=>setTimeout(r,800*(attempt+1)))}}if(alive)setState(s=>({...s,isLoading:false,error:'Unable to restore your session. Check your connection and reload.'}))}void restore();const{data:listener}=supabase.auth.onAuthStateChange((event,session)=>{if(!alive)return;if(['INITIAL_SESSION','SIGNED_IN','TOKEN_REFRESHED','USER_UPDATED'].includes(event)&&session?.user){if(handlingLoginRef.current&&event==='SIGNED_IN')return;void loadProfile(session.user.id,{preserveOnFailure:true})}else if(event==='SIGNED_OUT'){if(weTriggeredSignOutRef.current){weTriggeredSignOutRef.current=false;return}void wipeOnLogout();setState({page:'login',profile:null,isLoading:false,error:'',kickedOut:false})}});const onOnline=()=>{void supabase.auth.getSession().then(({data})=>{if(data.session?.user)void loadProfile(data.session.user.id,{preserveOnFailure:true})})};window.addEventListener('online',onOnline);document.addEventListener('visibilitychange',onOnline);return()=>{alive=false;listener.subscription.unsubscribe();window.removeEventListener('online',onOnline);document.removeEventListener('visibilitychange',onOnline)}},[loadProfile]);
const handleLoginSuccess=useCallback(async(authId:string,email:string,role?:'user'|'worker'|'property_partner')=>{handlingLoginRef.current=true;setState(s=>({...s,isLoading:true,error:''}));try{const{profile:byAuth}=await getProfileByAuthId(authId);const{profile:byEmail}=!byAuth?await getProfileByEmail(email):{profile:null};let p=byAuth;if(!p&&byEmail){if(!await allowEntry(byEmail))return;const{profile:linked,error}=await linkProfileToAuth(byEmail.user_id,authId);if(error||!linked)throw error||new Error('Could not link account');p=linked}if(!p){if(await maintenance())throw new Error('WeHouse is currently under maintenance.');if(await registrationClosed())throw new Error('New registrations are currently closed.');const{profile:newProfile,error}=await createProfile(authId,email,role||'user');if(error||!newProfile)throw error||new Error('Could not create account');p=newProfile;if((role||'user')==='property_partner')await supabase.from('property_partners').insert({profile_id:p.user_id,partner_code:`WHP-${Date.now().toString(36).toUpperCase()}`,status:'pending_verification'})}if(await allowEntry(p)){setState({profile:p,page:determinePage(p),isLoading:false,error:'',kickedOut:false});void trackSession(p.user_id,authId);void createUserSession(p.user_id,authId)}}catch(e:any){setState({page:'login',profile:null,isLoading:false,error:e?.message||'Login failed. Please try again.'})}finally{handlingLoginRef.current=false}},[allowEntry,determinePage]);
const handleSetupComplete=useCallback((p:Profile)=>setState({profile:p,page:determinePage(p),isLoading:false,error:''}),[determinePage]);const logout=useCallback(async()=>{const userId=state.profile?.user_id,authId=state.profile?.auth_id,sid=getStoredSessionId();if(sid)await deactivateUserSession(sid).catch(()=>{});if(userId&&authId)await endSession(userId,authId).catch(()=>{});weTriggeredSignOutRef.current=true;await supabase.auth.signOut({scope:'local'});await wipeOnLogout();setState({page:'login',profile:null,isLoading:false,error:'',kickedOut:false})},[state.profile]);
useEffect(()=>{if(!state.profile||['login','setup','worker_setup'].includes(state.page))return;let sid=getStoredSessionId();const uid=state.profile.user_id,aid=state.profile.auth_id;async function ensure(){if(sid)return;const{data}=await supabase.from('user_sessions').select('id').eq('user_id',uid).eq('is_active',true).order('created_at',{ascending:false}).limit(1).maybeSingle();sid=data?.id||await createUserSession(uid,aid)||null;if(sid)localStorage.setItem('wh_session_id',sid)}void ensure();const timer=setInterval(()=>{if(sid)void updateSessionLastSeen(sid).catch(()=>{});else void ensure()},60000);return()=>clearInterval(timer)},[state.profile?.user_id,state.page]);const clearError=useCallback(()=>setState(s=>({...s,error:''})),[]);return{...state,handleLoginSuccess,handleSetupComplete,logout,clearError,kickedOut:state.kickedOut}}
