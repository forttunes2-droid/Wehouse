import { useEffect, useRef, useState } from 'react';
import { Toaster, toast } from 'sonner';
import WorkerReadinessTest from '@/components/WorkerReadinessTest';
import { supabase } from '@/lib/supabase';
import { verifyPaymentWithRetry } from '@/lib/supabase/payment-verify';
import type { Profile } from '@/types';

type Props={profile:Profile;onBack:()=>void};
type Activation={worker_status:string;live:boolean;profile_complete:boolean;payment_status:string|null;gold_badge:boolean;test_passed:boolean;test_percent:number|null;test_attempts_24h:number;evidence_saved:boolean;submitted:boolean;review_status:string|null;identity_status:string;identity_provider:string|null;identity_checked_at:string|null;rejection_reason:string|null};
const EMPTY:Activation={worker_status:'pending',live:false,profile_complete:false,payment_status:null,gold_badge:false,test_passed:false,test_percent:null,test_attempts_24h:0,evidence_saved:false,submitted:false,review_status:null,identity_status:'not_started',identity_provider:null,identity_checked_at:null,rejection_reason:null};
const PAYMENT_REF_KEY='wh_worker_verification_payment_ref';

export default function WorkerVerificationPhase4({profile,onBack}:Props){
 const certificateInput=useRef<HTMLInputElement>(null),videoInput=useRef<HTMLInputElement>(null),selfieInput=useRef<HTMLInputElement>(null);
 const[activation,setActivation]=useState<Activation>(EMPTY),[fee,setFee]=useState(0),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[certificatePath,setCertificatePath]=useState(''),[videoPath,setVideoPath]=useState(''),[videoPreview,setVideoPreview]=useState(''),[vNin,setVNin]=useState(''),[selfie,setSelfie]=useState<File|null>(null),[consent,setConsent]=useState(false);
 async function refresh(){const[{data,error},feeResult]=await Promise.all([supabase.rpc('get_my_worker_activation'),supabase.rpc('get_setting_v2',{p_key:'worker_verification_fee'})]);if(error)toast.error(error.message);else setActivation({...EMPTY,...(data||{})} as Activation);setFee(Number(feeResult.data||0));setLoading(false)}
 useEffect(()=>{void refresh()},[profile.user_id]);
 useEffect(()=>()=>{if(videoPreview)URL.revokeObjectURL(videoPreview)},[videoPreview]);
 useEffect(()=>{if(activation.identity_status!=='pending_external')return;const timer=window.setInterval(()=>void refresh(),15000);return()=>window.clearInterval(timer)},[activation.identity_status]);
 useEffect(()=>{let cancelled=false;void(async()=>{let reference='';try{reference=localStorage.getItem(PAYMENT_REF_KEY)||''}catch{}if(!reference)return;const result=await verifyPaymentWithRetry(reference,{purpose:'worker_verification'});if(cancelled)return;if(result.success){try{localStorage.removeItem(PAYMENT_REF_KEY)}catch{}toast.success('Payment confirmed. Your Gold Tick is active.');await refresh()}else if(result.error&&!result.error.includes('Max retries')){toast.info('Payment has not been confirmed yet. You can retry from this page.')}})();return()=>{cancelled=true}},[]);
 async function pay(){if(fee<=0)return toast.error('Verification fee is not configured');setBusy(true);try{const{data:bootstrap,error:bootstrapError}=await supabase.rpc('create_worker_verification_payment');if(bootstrapError||!bootstrap?.success)throw new Error(bootstrap?.error||bootstrapError?.message||'Payment initialization failed');const reference=String(bootstrap.reference||'');if(!reference)throw new Error('Payment reference was not created');const{data,error}=await supabase.functions.invoke('worker-verification-payment-init',{body:{reference}});if(error)throw error;if(data?.already_paid){try{localStorage.removeItem(PAYMENT_REF_KEY)}catch{}toast.success('Payment already confirmed.');await refresh();return}if(!data?.success||!data?.authorization_url)throw new Error(data?.error||'Paystack could not initialize payment');try{localStorage.setItem(PAYMENT_REF_KEY,reference)}catch{}window.location.assign(String(data.authorization_url))}catch(e:any){toast.error(e?.message||'Payment could not be started');setBusy(false)}}
 async function upload(file:File,bucket:'worker-certificates'|'worker-verification-videos',kind:string){const ext=(file.name.split('.').pop()||'bin').toLowerCase(),path=`${profile.user_id}/${kind}-${Date.now()}.${ext}`;const{error}=await supabase.storage.from(bucket).upload(path,file,{contentType:file.type||undefined,upsert:false});if(error)throw error;return path}
 async function chooseCertificate(e:React.ChangeEvent<HTMLInputElement>){const file=e.target.files?.[0];if(!file)return;if(file.size>10*1024*1024)return toast.error('Certificate must be under 10MB');try{setCertificatePath(await upload(file,'worker-certificates','certificate'));toast.success('Certificate uploaded')}catch(err:any){toast.error(err?.message||'Certificate upload failed')}}
 async function chooseVideo(e:React.ChangeEvent<HTMLInputElement>){const file=e.target.files?.[0];if(!file)return;if(!file.type.startsWith('video/'))return toast.error('Choose a video file');if(file.size>50*1024*1024)return toast.error('Skill video must be under 50MB');try{const path=await upload(file,'worker-verification-videos','skill-video');setVideoPath(path);if(videoPreview)URL.revokeObjectURL(videoPreview);setVideoPreview(URL.createObjectURL(file));toast.success('Skill video uploaded')}catch(err:any){toast.error(err?.message||'Video upload failed')}}
 async function saveEvidence(){if(!videoPath)return toast.error('Skill demonstration video is required');setBusy(true);const{error}=await supabase.rpc('save_my_worker_professional_evidence',{p_certificate_path:certificatePath||null,p_video_path:videoPath});setBusy(false);if(error)return toast.error(error.message);toast.success('Professional evidence saved');await refresh()}
 function fileDataUrl(file:File){return new Promise<string>((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result||''));r.onerror=()=>reject(r.error||new Error('Could not read selfie'));r.readAsDataURL(file)})}
 async function verifyIdentity(){if(!vNin.trim())return toast.error('Enter your virtual NIN (vNIN)');if(!selfie)return toast.error('Take or choose a clear selfie');if(!consent)return toast.error('Consent is required');if(selfie.size>5*1024*1024)return toast.error('Selfie must be under 5MB');setBusy(true);try{const selfieImage=await fileDataUrl(selfie);const{data,error}=await supabase.functions.invoke('worker-identity-verify',{body:{vnin:vNin.trim(),selfieImage,isSubjectConsent:true}});if(error)throw error;if(!data?.success)throw new Error(data?.error||data?.reason||'Identity verification failed');if(data?.pending){toast.info('Youverify is still processing your identity check. WeHouse will update this automatically.');await refresh();return}if(!data?.verified)throw new Error(data?.reason||'Identity verification did not pass');toast.success('External identity verified');await refresh()}catch(e:any){toast.error(e?.message||'Identity verification failed')}finally{setBusy(false)}}
 async function submitReview(){setBusy(true);const{error}=await supabase.rpc('submit_my_worker_verification');setBusy(false);if(error)return toast.error(error.message);toast.success('Submitted for WeHouse review');await refresh()}
 if(loading)return <Screen><State title="Checking activation" text="Loading your Worker progress…"/></Screen>;
 const identityVerified=activation.identity_status==='verified',identityPending=activation.identity_status==='pending_external',underReview=activation.worker_status==='profile_under_review'||activation.submitted,live=activation.live||activation.worker_status==='verified';
 return <Screen><Toaster position="top-center" richColors theme="dark"/><Header onBack={onBack} gold={activation.gold_badge}/><main className="mx-auto max-w-3xl space-y-4 px-4 py-5 sm:px-5"><Progress activation={activation}/>{live?<Panel><State good title="Your Worker profile is live" text="You passed activation and can appear in Local Services while your account remains active."/><Button label="Back to Worker dashboard" onClick={onBack}/></Panel>:underReview?<Panel><State good title="Final WeHouse review" text="All Worker activation gates are complete. Your profile stays private until Verification Staff approves the professional review."/><Button label="Back to Worker dashboard" onClick={onBack}/></Panel>:!activation.profile_complete?<Panel><State title="Professional profile first" text="Complete your service, specialty, experience, price and service coverage from Profile before paying."/><Button label="Back to Worker dashboard" onClick={onBack}/></Panel>:!activation.gold_badge?<Panel><State title="Verification payment" text="A confirmed Paystack payment gives the Gold Tick. It confirms payment only and does not make your profile public."/><div className="rounded-2xl border border-white/[.07] bg-black/15 p-5 text-center"><p className="text-[9px] uppercase tracking-[.15em] text-[#6E7483]">Verification fee</p><p className="mt-2 text-3xl font-bold">₦{fee.toLocaleString()}</p></div><Primary label={busy?'Opening Paystack…':'Pay securely with Paystack'} disabled={busy||fee<=0} onClick={()=>void pay()}/></Panel>:!activation.test_passed?<WorkerReadinessTest onPassed={refresh}/>:!activation.evidence_saved?<Panel><State good title="Readiness test passed" text={`Now submit professional evidence${activation.test_percent!=null?` · test score ${activation.test_percent}%`:''}. The skill demonstration video is required.`}/><Upload label={certificatePath?'Certificate uploaded':'Professional certificate · optional'} done={!!certificatePath} onClick={()=>certificateInput.current?.click()}/><input ref={certificateInput} type="file" accept="image/jpeg,image/png,image/webp,application/pdf" className="hidden" onChange={chooseCertificate}/><Upload label={videoPath?'Skill video uploaded':'Skill demonstration video · required'} done={!!videoPath} onClick={()=>videoInput.current?.click()}/><input ref={videoInput} type="file" accept="video/mp4,video/webm,video/quicktime" className="hidden" onChange={chooseVideo}/>{videoPreview&&<video src={videoPreview} controls playsInline className="max-h-72 w-full rounded-2xl bg-black object-contain"/>}<p className="text-[10px] leading-relaxed text-[#777E8E]">This evidence is for approval. Public Work Stories and Portfolio are separate features available after activation.</p><Primary label={busy?'Saving…':'Save professional evidence'} disabled={busy||!videoPath} onClick={()=>void saveEvidence()}/></Panel>:identityPending?<Panel><State title="Identity check pending" text="Youverify accepted your identity request but the government-source result is still processing. You do not need to upload your vNIN again. WeHouse will accept the signed provider webhook when the check completes."/><div className="rounded-2xl border border-amber-400/15 bg-amber-400/[.05] p-4 text-[11px] leading-relaxed text-amber-200">Your Worker profile remains private until this identity gate passes and final WeHouse review is completed.</div><Button label="Check identity status now" onClick={()=>void refresh()}/></Panel>:!identityVerified?<Panel><State title="External government identity" text="Youverify checks your vNIN and selfie. WeHouse Staff do not inspect a government-ID upload; WeHouse keeps the provider result/reference instead."/><label className="block"><span className="mb-1.5 block text-[10px] text-[#8C92A1]">Virtual NIN (vNIN)</span><input value={vNin} onChange={e=>setVNin(e.target.value)} autoComplete="off" autoCapitalize="characters" className="h-12 w-full rounded-xl border border-white/[.08] bg-[#151922] px-3 text-sm outline-none" placeholder="Enter vNIN"/></label><Upload label={selfie?'Selfie selected':'Take or choose a clear selfie'} done={!!selfie} onClick={()=>selfieInput.current?.click()}/><input ref={selfieInput} type="file" accept="image/jpeg,image/png,image/webp" capture="user" className="hidden" onChange={e=>setSelfie(e.target.files?.[0]||null)}/><button type="button" onClick={()=>setConsent(v=>!v)} className="flex w-full items-start gap-3 rounded-2xl border border-white/[.07] bg-black/10 p-4 text-left"><span className={`grid h-6 w-6 shrink-0 place-items-center rounded-lg border ${consent?'border-cyan-500 bg-cyan-500 text-[#031013]':'border-white/15'}`}>{consent?'✓':''}</span><span className="text-[11px] leading-relaxed text-[#9298A7]">I consent to external identity verification and selfie comparison for my Worker application.</span></button><Primary label={busy?'Checking identity…':'Verify identity'} disabled={busy||!consent||!selfie||!vNin.trim()} onClick={()=>void verifyIdentity()}/></Panel>:<Panel><State good title="Identity verified" text="Your external identity gate passed. Submit the completed professional application for final WeHouse review."/><Primary label={busy?'Submitting…':'Submit for final review'} disabled={busy} onClick={()=>void submitReview()}/></Panel>}</main></Screen>}
function Screen({children}:{children:React.ReactNode}){return <div className="min-h-[100dvh] bg-[#080A0F] pb-8 text-white">{children}</div>}
function Header({onBack,gold}:{onBack:()=>void;gold:boolean}){return <header className="sticky top-0 z-30 border-b border-white/[.06] bg-[#080A0F]/95 px-4 py-4 backdrop-blur-xl"><div className="mx-auto flex max-w-3xl items-center gap-3"><button onClick={onBack} className="grid h-10 w-10 place-items-center rounded-xl border border-white/[.07] bg-white/[.03]">←</button><div className="min-w-0 flex-1"><p className="text-[9px] font-bold tracking-[.2em] text-cyan-300">WORKER ACTIVATION</p><h1 className="mt-1 text-lg font-bold">Verification</h1></div>{gold&&<span className="rounded-full border border-amber-400/20 bg-amber-400/[.08] px-2.5 py-1 text-[9px] font-bold text-amber-300">GOLD TICK</span>}</div></header>}
function Panel({children}:{children:React.ReactNode}){return <section className="space-y-4 rounded-3xl border border-white/[.07] bg-[#11151D] p-4 sm:p-5">{children}</section>}
function State({title,text,good=false}:{title:string;text:string;good?:boolean}){return <div><p className={`text-sm font-semibold ${good?'text-emerald-300':'text-white'}`}>{title}</p><p className="mt-1 text-[11px] leading-relaxed text-[#777E8E]">{text}</p></div>}
function Primary({label,onClick,disabled=false}:{label:string;onClick:()=>void;disabled?:boolean}){return <button type="button" onClick={onClick} disabled={disabled} className="h-12 w-full rounded-2xl bg-cyan-500 text-xs font-semibold text-[#041014] disabled:opacity-40">{label}</button>}
function Button({label,onClick}:{label:string;onClick:()=>void}){return <button type="button" onClick={onClick} className="h-11 w-full rounded-2xl border border-white/[.08] text-xs font-semibold">{label}</button>}
function Upload({label,done,onClick}:{label:string;done:boolean;onClick:()=>void}){return <button type="button" onClick={onClick} className={`flex min-h-12 w-full items-center justify-between rounded-2xl border px-4 text-left text-xs ${done?'border-emerald-500/20 bg-emerald-500/[.05] text-emerald-300':'border-white/[.08] bg-black/10 text-[#A2A7B3]'}`}><span>{label}</span><span>{done?'✓':'＋'}</span></button>}
function Progress({activation:a}:{activation:Activation}){const steps=[['Profile',a.profile_complete],['Gold Tick',a.gold_badge],['Test',a.test_passed],['Evidence',a.evidence_saved],['Identity',a.identity_status==='verified'],['Review',a.live]] as const;return <section className="rounded-3xl border border-white/[.07] bg-[#0F131A] p-4"><div className="grid grid-cols-6 gap-1.5">{steps.map(([label,done],i)=><div key={label} className="min-w-0 text-center"><div className={`mx-auto grid h-7 w-7 place-items-center rounded-full text-[9px] font-bold ${done?'bg-emerald-500 text-[#04100B]':'border border-white/[.1] bg-white/[.03] text-[#697080]'}`}>{done?'✓':i+1}</div><p className={`mt-1 truncate text-[8px] ${done?'text-emerald-300':'text-[#626979]'}`}>{label}</p></div>)}</div></section>}
