import { useEffect, useState } from 'react';
import { Toaster, toast } from 'sonner';
import { supabase, getHotels, createHotel, updateHotel, deleteHotel, createHotelRoom, deleteHotelRoom, getHotelRooms } from '@/lib/supabase';
import { useCreatorAuth } from '@/hooks/useCreatorAuth';
import UserProfileModal from '@/components/UserProfileModal';
import StaffListTab from './StaffListTab';
import CreatorSettingsTab from './CreatorSettingsTab';
import AnalyticsPage from './AnalyticsPage';
import { AnnouncementsTab } from '@/components/AnnouncementsTab';
import DomainSettingsPanel from '@/components/DomainSettingsPanel';
import ServiceCategoryManager from '@/components/ServiceCategoryManager';
import PropertyTypeManager from '@/components/PropertyTypeManager';
import type { Profile, HotelRoom } from '@/types';

type Tab='overview'|'management'|'finance'|'analytics'|'settings';
type ManagementTab='people'|'staff'|'properties'|'workers'|'bookings'|'support'|'reports'|'announcements';
type Props={profile:Profile;onLogout:()=>void;onGoToNewListing?:()=>void;onNavigate?:(page:string)=>void;onGoToChat?:(id?:string)=>void};
const TABS:Array<{id:Tab;label:string;note:string}>=[
 {id:'overview',label:'Overview',note:'Platform health, priorities and shortcuts'},
 {id:'management',label:'Management',note:'People, staff, properties, workers, bookings and platform operations'},
 {id:'finance',label:'Finance',note:'Canonical commissions, withdrawals and financial rules'},
 {id:'analytics',label:'Analytics',note:'Platform performance, activity and reporting'},
 {id:'settings',label:'Settings',note:'Global configuration, categories and platform controls'},
];
const MANAGEMENT:Array<{id:ManagementTab;label:string;note:string}>=[
 {id:'people',label:'People',note:'Users, workers, partners and Admin accounts'},
 {id:'staff',label:'Staff',note:'Staff accounts, branches and module assignments'},
 {id:'properties',label:'Properties',note:'Apartments, hotels and listing operations'},
 {id:'workers',label:'Worker Review',note:'Worker verification and public status'},
 {id:'bookings',label:'Bookings',note:'Worker, apartment and hotel bookings'},
 {id:'support',label:'Support',note:'Platform support conversations'},
 {id:'reports',label:'Reports',note:'Listing reports and moderation'},
 {id:'announcements',label:'Announcements',note:'Platform-wide communication'},
];

export default function CreatorDashboard({profile,onLogout,onGoToNewListing,onNavigate,onGoToChat}:Props){
 const {clearAuth}=useCreatorAuth();const [tab,setTab]=useState<Tab>('overview');const [management,setManagement]=useState<ManagementTab>('people');const [viewing,setViewing]=useState<Profile|null>(null);const current=TABS.find(t=>t.id===tab)!;
 function logout(){clearAuth();onLogout()}
 function openManagement(section:ManagementTab){setManagement(section);setTab('management')}
 return <div className="min-h-[100dvh] bg-[#070910] text-white"><Toaster position="top-center" richColors/>
  <header className="sticky top-0 z-40 border-b border-white/[0.06] bg-[#080A12]/95 backdrop-blur-xl"><div className="mx-auto max-w-[1500px] px-4 pt-5 lg:px-8"><div className="flex items-start justify-between gap-4"><div className="min-w-0"><p className="text-[9px] font-bold tracking-[.25em] text-violet-400">WEHOUSE CREATOR</p><h1 className="mt-1 text-xl font-bold">{current.label}</h1><p className="mt-1 text-[10px] text-[#74798B]">{current.note}</p></div><div className="flex shrink-0 gap-2">{onNavigate&&<button onClick={()=>onNavigate('profile')} className="rounded-xl border border-white/[0.08] bg-white/[0.04] px-3 py-2 text-[10px] text-[#A3A6B5]">Account</button>}<button onClick={logout} className="rounded-xl border border-red-500/15 bg-red-500/[0.06] px-3 py-2 text-[10px] text-red-300">Log out</button></div></div><div className="mt-4 overflow-x-auto pb-3 scrollbar-hide"><div className="flex min-w-max gap-1">{TABS.map(item=><button key={item.id} onClick={()=>setTab(item.id)} className={`rounded-xl px-4 py-2 text-[10px] font-semibold ${tab===item.id?'bg-violet-500 text-white':'text-[#74798B] hover:bg-white/[0.04] hover:text-white'}`}>{item.label}</button>)}</div></div></div></header>
  <main className="mx-auto max-w-[1500px] px-4 py-6 pb-24 lg:px-8 lg:py-8">
   {tab==='overview'&&<Overview openManagement={openManagement} openFinance={()=>setTab('finance')} openAnalytics={()=>setTab('analytics')}/>} 
   {tab==='management'&&<Management profile={profile} active={management} setActive={setManagement} onView={setViewing} onGoToNewListing={onGoToNewListing} onGoToChat={onGoToChat}/>} 
   {tab==='finance'&&<Finance/>} 
   {tab==='analytics'&&<AnalyticsPage profile={profile}/>} 
   {tab==='settings'&&<Settings profile={profile}/>} 
  </main>
  {viewing&&<UserProfileModal user={viewing} adminProfile={profile} onClose={()=>setViewing(null)} onNavigate={onNavigate} onGoToChat={onGoToChat}/>} 
 </div>
}

function Overview({openManagement,openFinance,openAnalytics}:{openManagement:(tab:ManagementTab)=>void;openFinance:()=>void;openAnalytics:()=>void}){
 const [stats,setStats]=useState<any>({users:0,workers:0,partners:0,staff:0,listings:0,pending_verifications:0,hotels:0,pendingWithdrawals:0,pendingInspections:0});const [loading,setLoading]=useState(true);
 useEffect(()=>{void(async()=>{const [base,hotels,withdrawals,inspections]=await Promise.all([supabase.rpc('admin_get_my_branch_stats'),supabase.from('hotels').select('*',{count:'exact',head:true}),supabase.from('withdrawals').select('*',{count:'exact',head:true}).in('status',['pending','processing']),supabase.from('inspection_requests').select('*',{count:'exact',head:true}).in('status',['pending','scheduled','in_progress'])]);setStats({...((base.data||{}) as any),hotels:hotels.count||0,pendingWithdrawals:withdrawals.count||0,pendingInspections:inspections.count||0});setLoading(false)})()},[]);
 if(loading)return <Loading/>;
 const cards:Array<[string,number,()=>void,string]>=[
  ['Users',stats.users,()=>openManagement('people'),'Customer accounts'],['Workers',stats.workers,()=>openManagement('workers'),'Service providers'],['Property partners',stats.partners,()=>openManagement('people'),'Property owners'],['Staff',stats.staff,()=>openManagement('staff'),'Operational team'],['Apartment listings',stats.listings,()=>openManagement('properties'),'Canonical listings'],['Hotels',stats.hotels,()=>openManagement('properties'),'Hotel inventory'],['Worker reviews',stats.pending_verifications,()=>openManagement('workers'),'Awaiting review'],['Property inspections',stats.pendingInspections,()=>openManagement('properties'),'Pending field work'],['Withdrawals',stats.pendingWithdrawals,openFinance,'Pending finance work']
 ];
 return <div className="space-y-6"><section className="overflow-hidden rounded-3xl border border-violet-500/15 bg-gradient-to-br from-violet-500/[0.14] via-[#14111F] to-[#0E1118] p-6 lg:p-8"><span className="rounded-full bg-violet-500/10 px-3 py-1 text-[9px] font-semibold text-violet-300">PLATFORM COMMAND CENTER</span><h2 className="mt-4 max-w-3xl text-3xl font-bold lg:text-4xl">Everything has one clear home.</h2><p className="mt-3 max-w-2xl text-xs leading-relaxed text-[#9498A9]">Operations live under Management. Money lives under Finance. Performance lives under Analytics. Platform configuration lives under Settings.</p><div className="mt-5 flex flex-wrap gap-2"><button onClick={()=>openManagement('people')} className="rounded-xl bg-violet-500 px-4 py-3 text-xs font-semibold">Open Management</button><button onClick={openFinance} className="rounded-xl border border-white/[0.08] bg-white/[0.04] px-4 py-3 text-xs font-semibold">Open Finance</button><button onClick={openAnalytics} className="rounded-xl border border-white/[0.08] bg-white/[0.04] px-4 py-3 text-xs font-semibold">Open Analytics</button></div></section><section className="grid grid-cols-2 gap-3 md:grid-cols-3 xl:grid-cols-5">{cards.map(([label,value,action,note])=><button key={label} onClick={action} className="rounded-2xl border border-white/[0.06] bg-[#10131B] p-4 text-left hover:border-violet-500/25"><p className="text-2xl font-bold">{value||0}</p><p className="mt-1 text-[10px] font-semibold text-white">{label}</p><p className="mt-1 text-[9px] text-[#616678]">{note}</p></button>)}</section></div>
}

function Management({profile,active,setActive,onView,onGoToNewListing,onGoToChat}:{profile:Profile;active:ManagementTab;setActive:(tab:ManagementTab)=>void;onView:(p:Profile)=>void;onGoToNewListing?:()=>void;onGoToChat?:(id?:string)=>void}){
 const current=MANAGEMENT.find(item=>item.id===active)!;
 return <div className="space-y-5"><div><h2 className="text-lg font-bold">Management</h2><p className="mt-1 text-[10px] text-[#707386]">Operational control is grouped here instead of appearing as competing top-level dashboards.</p></div><div className="overflow-x-auto scrollbar-hide"><div className="flex min-w-max gap-1 rounded-2xl border border-white/[0.05] bg-[#0D1017] p-1">{MANAGEMENT.map(item=><button key={item.id} onClick={()=>setActive(item.id)} className={`rounded-xl px-3 py-2 text-[10px] font-semibold ${active===item.id?'bg-violet-500 text-white':'text-[#777A8C] hover:text-white'}`}>{item.label}</button>)}</div></div><div className="rounded-2xl border border-violet-500/10 bg-violet-500/[0.03] p-3"><p className="text-xs font-semibold">{current.label}</p><p className="mt-1 text-[9px] text-[#6E7183]">{current.note}</p></div>{active==='people'&&<People onView={onView}/>} {active==='staff'&&<StaffListTab profile={profile}/>} {active==='properties'&&<Properties profile={profile} onGoToNewListing={onGoToNewListing}/>} {active==='workers'&&<Workers/>} {active==='bookings'&&<Bookings/>} {active==='support'&&<Support onOpen={onGoToChat}/>} {active==='reports'&&<Reports/>} {active==='announcements'&&<AnnouncementsTab profile={profile} scope="all"/>}</div>
}

function People({onView}:{onView:(p:Profile)=>void}){const [role,setRole]=useState('user'),[rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true),[search,setSearch]=useState('');useEffect(()=>{void load()},[role]);async function load(){setLoading(true);const {data,error}=await supabase.rpc('admin_get_my_branch_profiles',{p_role:role});if(error)toast.error(error.message);setRows(Array.isArray(data)?data:[]);setLoading(false)}const shown=rows.filter(r=>!search.trim()||[r.full_name,r.username,r.email,r.user_id].filter(Boolean).join(' ').toLowerCase().includes(search.toLowerCase()));return <Section title="People" note="Global Creator view. Open a profile for account actions and role-specific details."><div className="flex flex-wrap gap-2">{[['user','Users'],['worker','Workers'],['property_partner','Property Partners'],['admin','Admins']].map(([id,label])=><Chip key={id} active={role===id} onClick={()=>setRole(id)}>{label}</Chip>)}</div><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search people" className="h-11 w-full rounded-xl border border-white/[0.08] bg-[#141720] px-3 text-xs outline-none focus:border-violet-500/40"/>{loading?<Loading/>:shown.length===0?<Empty title="No matching accounts" text="No accounts match this filter."/>:<Grid>{shown.map(p=><button key={p.user_id} onClick={()=>onView(p)} className="rounded-2xl border border-white/[0.06] bg-[#10131B] p-4 text-left hover:border-violet-500/25"><div className="flex items-center gap-3"><Avatar text={p.full_name||p.username||p.email}/><div className="min-w-0"><p className="truncate text-sm font-semibold">{p.full_name||p.username||'WeHouse user'}</p><p className="truncate text-[10px] text-[#6D7082]">{p.email}</p><p className="mt-1 text-[9px] capitalize text-[#535667]">{p.role?.replace(/_/g,' ')} · {[p.local_government||p.city,p.state].filter(Boolean).join(', ')||'Location not set'}</p></div></div></button>)}</Grid>}</Section>}
function Properties({profile,onGoToNewListing}:{profile:Profile;onGoToNewListing?:()=>void}){const [view,setView]=useState<'apartments'|'hotels'>('apartments');return <Section title="Properties" note="WeHouse controls public apartment and hotel inventory. Property Partners submit requests; they do not publish directly." right={<Segment value={view} set={v=>setView(v as any)} items={[['apartments','Apartments'],['hotels','Hotels']]}/>}>{view==='apartments'?<ApartmentManager onAdd={onGoToNewListing}/>:<HotelManager profile={profile}/>}</Section>}
function ApartmentManager({onAdd}:{onAdd?:()=>void}){const [rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true),[filter,setFilter]=useState('all'),[reason,setReason]=useState<Record<string,string>>({});async function load(){setLoading(true);const {data,error}=await supabase.rpc('admin_get_my_branch_listings',{p_status:filter});if(error)toast.error(error.message);setRows(Array.isArray(data)?data:[]);setLoading(false)}useEffect(()=>{void load()},[filter]);async function review(id:string,decision:'approve'|'reject'){const {error}=await supabase.rpc('admin_review_my_branch_listing',{p_listing_id:id,p_decision:decision,p_reason:decision==='reject'?(reason[id]||null):null});if(error)return toast.error(error.message);toast.success(decision==='approve'?'Listing published':'Listing rejected');void load()}return <div className="space-y-4"><div className="flex flex-wrap items-center justify-between gap-2"><div className="flex gap-2">{[['all','All'],['pending_approval','Pending'],['available','Live'],['rejected','Rejected']].map(([id,label])=><Chip key={id} active={filter===id} onClick={()=>setFilter(id)}>{label}</Chip>)}</div>{onAdd&&<button onClick={onAdd} className="rounded-xl bg-violet-500 px-4 py-2.5 text-[10px] font-semibold">Create WeHouse listing</button>}</div>{loading?<Loading/>:rows.length===0?<Empty title="No apartment listings" text="Nothing matches this status."/>:<Grid>{rows.map(l=><Card key={l.id}><Top title={l.title||'Apartment'} sub={`${[l.city,l.state].filter(Boolean).join(', ')} · ${money(l.price)}`} status={l.status}/>{l.status==='pending_approval'&&<div className="mt-4 space-y-2"><input value={reason[l.id]||''} onChange={e=>setReason(v=>({...v,[l.id]:e.target.value}))} placeholder="Rejection reason if rejecting" className="h-10 w-full rounded-xl border border-white/[0.08] bg-[#171A23] px-3 text-[10px]"/><div className="flex gap-2"><Btn onClick={()=>void review(l.id,'approve')}>Approve</Btn><Btn danger onClick={()=>void review(l.id,'reject')}>Reject</Btn></div></div>}</Card>)}</Grid>}</div>}
function HotelManager({profile}:{profile:Profile}){const [hotels,setHotels]=useState<any[]>([]),[loading,setLoading]=useState(true),[adding,setAdding]=useState(false),[selected,setSelected]=useState<any|null>(null),[form,setForm]=useState({name:'',state:'',city:'',address:''});async function load(){setLoading(true);const {data,error}=await supabase.from('hotels').select('*,hotel_rooms(*)').order('created_at',{ascending:false});if(error)toast.error(error.message);setHotels(data||[]);setLoading(false)}useEffect(()=>{void load()},[]);async function add(){if(!form.name.trim()||!form.state.trim()||!form.city.trim())return toast.error('Hotel name, state and city are required');const {error}=await createHotel({name:form.name.trim(),description:null,state:form.state.trim(),city:form.city.trim(),area:null,address:form.address.trim()||null,images:[],amenities:[],owner_id:profile.user_id,status:'active',featured:false});if(error)return toast.error(error.message);toast.success('Hotel created');setAdding(false);setForm({name:'',state:'',city:'',address:''});void load()}async function remove(id:number){const {error}=await deleteHotel(id);if(error)return toast.error(error.message);toast.success('Hotel removed');setSelected(null);void load()}if(selected)return <HotelDetails hotel={selected} onBack={()=>{setSelected(null);void load()}} onDelete={()=>void remove(selected.hotel_id)}/>;return <div className="space-y-4"><div className="flex justify-end"><button onClick={()=>setAdding(v=>!v)} className="rounded-xl bg-violet-500 px-4 py-2.5 text-[10px] font-semibold">{adding?'Cancel':'Add hotel'}</button></div>{adding&&<Card><div className="grid gap-2 md:grid-cols-2"><Field value={form.name} set={v=>setForm({...form,name:v})} placeholder="Hotel name"/><Field value={form.state} set={v=>setForm({...form,state:v})} placeholder="State"/><Field value={form.city} set={v=>setForm({...form,city:v})} placeholder="City / LGA"/><Field value={form.address} set={v=>setForm({...form,address:v})} placeholder="Address"/></div><div className="mt-3"><Btn onClick={()=>void add()}>Create hotel</Btn></div></Card>}{loading?<Loading/>:hotels.length===0?<Empty title="No hotels" text="Create the first WeHouse hotel record."/>:<Grid>{hotels.map(h=><button key={h.hotel_id} onClick={()=>setSelected(h)} className="overflow-hidden rounded-2xl border border-white/[0.06] bg-[#10131B] text-left hover:border-violet-500/25">{h.images?.[0]?<img src={h.images[0]} alt="" className="h-36 w-full object-cover"/>:<div className="grid h-36 place-items-center bg-[#151923] text-3xl">🏨</div>}<div className="p-4"><Top title={h.name} sub={`${h.city}, ${h.state} · ${(h.hotel_rooms||[]).length} room types`} status={h.status}/></div></button>)}</Grid>}</div>}
function HotelDetails({hotel,onBack,onDelete}:{hotel:any;onBack:()=>void;onDelete:()=>void}){const [rooms,setRooms]=useState<HotelRoom[]>([]),[loading,setLoading]=useState(true),[featured,setFeatured]=useState(Boolean(hotel.featured)),[room,setRoom]=useState({room_type:'',price:'',max_guests:'2',total_rooms:'1'});async function load(){const {rooms,error}=await getHotelRooms(hotel.hotel_id);if(error)toast.error(error.message);setRooms(rooms||[]);setLoading(false)}useEffect(()=>{void load()},[hotel.hotel_id]);async function toggleFeatured(){const {error}=await updateHotel(hotel.hotel_id,{featured:!featured});if(error)return toast.error(error.message);setFeatured(!featured);toast.success('Hotel updated')}async function addRoom(){if(!room.room_type.trim()||Number(room.price)<=0)return toast.error('Room type and price are required');const {error}=await createHotelRoom({hotel_id:hotel.hotel_id,room_type:room.room_type.trim(),description:null,price_per_night:Number(room.price),max_guests:Number(room.max_guests)||2,bed_type:null,images:[],amenities:[],total_rooms:Number(room.total_rooms)||1});if(error)return toast.error(error.message);toast.success('Room type added');setRoom({room_type:'',price:'',max_guests:'2',total_rooms:'1'});void load()}async function removeRoom(id:number){const {error}=await deleteHotelRoom(id);if(error)return toast.error(error.message);toast.success('Room removed');void load()}return <div className="space-y-4"><button onClick={onBack} className="text-[10px] font-semibold text-violet-400">← Back to hotels</button><Card><Top title={hotel.name} sub={`${hotel.city}, ${hotel.state}`} status={hotel.status}/><div className="mt-4 flex gap-2"><Btn onClick={()=>void toggleFeatured()}>{featured?'Remove featured':'Feature hotel'}</Btn><Btn danger onClick={onDelete}>Delete hotel</Btn></div></Card><Card><h3 className="text-sm font-semibold">Add room type</h3><div className="mt-3 grid gap-2 md:grid-cols-4"><Field value={room.room_type} set={v=>setRoom({...room,room_type:v})} placeholder="Room type"/><Field value={room.price} set={v=>setRoom({...room,price:v})} placeholder="Price/night" type="number"/><Field value={room.max_guests} set={v=>setRoom({...room,max_guests:v})} placeholder="Max guests" type="number"/><Field value={room.total_rooms} set={v=>setRoom({...room,total_rooms:v})} placeholder="Total rooms" type="number"/></div><div className="mt-3"><Btn onClick={()=>void addRoom()}>Add room</Btn></div></Card>{loading?<Loading/>:<div className="space-y-2">{rooms.map(r=><Card key={r.room_id}><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold">{r.room_type}</p><p className="mt-1 text-[9px] text-[#676B7D]">{money(r.price_per_night)} / night · {r.total_rooms} rooms · {r.max_guests} guests</p></div><button onClick={()=>void removeRoom(r.room_id)} className="rounded-lg border border-red-500/20 bg-red-500/10 px-3 py-2 text-[9px] text-red-300">Remove</button></div></Card>)}</div>}</div>}
function Workers(){const [rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true),[filter,setFilter]=useState('all'),[selected,setSelected]=useState<any|null>(null),[reason,setReason]=useState('');async function load(){setLoading(true);const {data,error}=await supabase.rpc('admin_get_my_branch_profiles',{p_role:'worker'});if(error)toast.error(error.message);setRows(Array.isArray(data)?data:[]);setLoading(false)}useEffect(()=>{void load()},[]);const shown=filter==='all'?rows:rows.filter(w=>w.worker_status===filter);async function review(id:string,decision:'approve'|'reject'){const {error}=await supabase.rpc('admin_review_my_branch_worker',{p_worker_id:id,p_decision:decision,p_reason:decision==='reject'?reason:null});if(error)return toast.error(error.message);toast.success(decision==='approve'?'Worker verified':'Worker rejected');setSelected(null);setReason('');void load()}if(selected)return <Section title="Worker evidence" note="Review submitted identity and skill evidence before deciding."><button onClick={()=>setSelected(null)} className="text-[10px] text-violet-400">← Back to workers</button><Card><Top title={selected.full_name||selected.username||'Worker'} sub={`${selected.worker_occupation||'Occupation not set'} · ${[selected.local_government||selected.city,selected.state].filter(Boolean).join(', ')}`} status={selected.worker_status}/><div className="mt-4 grid gap-3 md:grid-cols-2">{selected.worker_gov_id_url?<Evidence title="Government ID" url={selected.worker_gov_id_url}/>:<Missing label="Government ID"/>}{selected.worker_video_url?<Video title="Skill video" url={selected.worker_video_url}/>:<Missing label="Skill video"/>}</div>{selected.worker_status==='profile_under_review'&&<div className="mt-4 space-y-2"><Field value={reason} set={setReason} placeholder="Rejection reason if rejecting"/><div className="flex gap-2"><Btn onClick={()=>void review(selected.user_id,'approve')}>Approve & publish</Btn><Btn danger onClick={()=>void review(selected.user_id,'reject')}>Reject</Btn></div></div>}</Card></Section>;return <Section title="Workers" note="Verification status and evidence review. Worker service setup remains with the worker."><div className="flex gap-2 overflow-x-auto scrollbar-hide">{[['all','All'],['pending','Pending'],['verification_paid','Verification paid'],['profile_under_review','Under review'],['verified','Verified'],['rejected','Rejected'],['suspended','Suspended']].map(([id,label])=><Chip key={id} active={filter===id} onClick={()=>setFilter(id)}>{label}</Chip>)}</div>{loading?<Loading/>:shown.length===0?<Empty title="No workers" text="No workers match this stage."/>:<Grid>{shown.map(w=><button key={w.user_id} onClick={()=>setSelected(w)} className="rounded-2xl border border-white/[0.06] bg-[#10131B] p-4 text-left hover:border-violet-500/25"><Top title={w.full_name||w.username||'Worker'} sub={`${w.worker_occupation||'Occupation not set'} · ${[w.local_government||w.city,w.state].filter(Boolean).join(', ')}`} status={w.worker_status}/><p className="mt-3 text-[9px] font-semibold text-violet-400">OPEN PROFILE →</p></button>)}</Grid>}</Section>}
function Bookings(){const [view,setView]=useState<'worker'|'apartments'|'hotels'>('worker'),[rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{void load()},[view]);async function load(){setLoading(true);let data:any[]=[];let error:any=null;if(view==='worker'){const r=await supabase.rpc('admin_get_my_branch_worker_bookings');data=Array.isArray(r.data)?r.data:[];error=r.error}else if(view==='apartments'){const r=await supabase.from('reservations').select('*').order('created_at',{ascending:false}).limit(100);data=r.data||[];error=r.error}else{const r=await supabase.from('hotel_bookings').select('*,hotels(name,city,state),hotel_rooms(room_type)').order('created_at',{ascending:false}).limit(100);data=r.data||[];error=r.error}if(error)toast.error(error.message);setRows(data);setLoading(false)}return <Section title="Bookings" note="Global Creator monitoring across worker services, apartments and hotels." right={<Segment value={view} set={v=>setView(v as any)} items={[['worker','Workers'],['apartments','Apartments'],['hotels','Hotels']]}/>}>{loading?<Loading/>:rows.length===0?<Empty title="No bookings" text="No records found in this booking type."/>:<div className="space-y-3">{rows.map((r:any)=><Card key={r.id||r.booking_id}><Top title={view==='worker'?(r.service_type||r.booking_code||'Worker booking'):view==='hotels'?(r.hotels?.name||'Hotel booking'):(r.reservation_code||r.listing_id||'Apartment reservation')} sub={new Date(r.created_at).toLocaleString()} status={r.status||'recorded'} right={money(r.negotiated_amount||r.agreed_amount||r.total_price||r.amount||0)}/></Card>)}</div>}</Section>}
function Finance(){const [view,setView]=useState<'withdrawals'|'commissions'>('withdrawals'),[rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{void load()},[view]);async function load(){setLoading(true);const query=view==='withdrawals'?supabase.from('withdrawals').select('*').order('created_at',{ascending:false}).limit(100):supabase.from('commission_ledger').select('*').order('created_at',{ascending:false}).limit(100);const {data,error}=await query;if(error)toast.error(error.message);setRows(data||[]);setLoading(false)}const total=rows.reduce((s,r)=>s+Number(view==='withdrawals'?r.amount:r.commission_amount||0),0);return <Section title="Finance" note="Canonical financial records and Creator-owned financial configuration." right={<Segment value={view} set={v=>setView(v as any)} items={[['withdrawals','Withdrawals'],['commissions','Commissions']]}/>}><div className="grid gap-4 lg:grid-cols-[1.4fr_1fr]"><div className="space-y-4"><Stats items={[['Records',rows.length],['Value',money(total)]]}/>{loading?<Loading/>:rows.length===0?<Empty title="No finance records" text="No records are available."/>:<div className="space-y-2">{rows.map(r=><Card key={r.id}><Top title={view==='withdrawals'?(r.snapshot_bank_account_name||'Withdrawal'):(r.booking_type||'Commission')} sub={new Date(r.created_at).toLocaleString()} status={r.status||'recorded'} right={money(view==='withdrawals'?r.amount:r.commission_amount)}/></Card>)}</div>}</div><div className="space-y-4"><DomainSettingsPanel title="Financial Rules" description="Canonical platform rates and withdrawal rules" settings={[{key:'worker_commission_rate',label:'Worker Commission (%)',description:'WeHouse commission on worker jobs',type:'number',defaultValue:'15'},{key:'property_partner_commission_rate',label:'Property Partner Commission (%)',description:'WeHouse commission on partner property earnings',type:'number',defaultValue:'10'},{key:'hotel_commission_rate',label:'Hotel Commission (%)',description:'WeHouse commission on hotel bookings',type:'number',defaultValue:'10'},{key:'wallet_minimum_withdrawal',label:'Minimum Withdrawal (₦)',description:'Minimum available balance required for withdrawal',type:'number',defaultValue:'5000'}]}/></div></div></Section>}
function Support({onOpen}:{onOpen?:(id?:string)=>void}){const [rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{void(async()=>{const {data,error}=await supabase.rpc('admin_support_inbox');if(error)toast.error(error.message);setRows(data||[]);setLoading(false)})()},[]);return <Section title="Support" note="Global support inbox. Creator can inspect any support conversation.">{loading?<Loading/>:rows.length===0?<Empty title="Support inbox clear" text="No active support conversations."/>:<div className="space-y-2">{rows.map(c=><button key={c.id} onClick={()=>onOpen?.(c.id)} className="w-full rounded-2xl border border-white/[0.06] bg-[#10131B] p-4 text-left hover:border-violet-500/25"><div className="flex justify-between gap-3"><div><p className="text-xs font-semibold">{c.user_name||c.user_email||'WeHouse user'}</p><p className="mt-1 text-[10px] text-[#6D7082]">{c.last_message||'Open conversation'}</p><p className="mt-1 text-[9px] text-[#535667]">{c.conversation_type?.replace(/_/g,' ')}</p></div>{Number(c.unread_b||0)>0&&<span className="h-fit rounded-full bg-violet-500 px-2 py-1 text-[8px] font-bold">{c.unread_b}</span>}</div></button>)}</div>}</Section>}
function Reports(){const [rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true);async function load(){setLoading(true);const {data,error}=await supabase.rpc('admin_get_my_branch_reports');if(error)toast.error(error.message);setRows(Array.isArray(data)?data:[]);setLoading(false)}useEffect(()=>{void load()},[]);async function act(id:string,action:'resolved'|'dismissed'){const {error}=await supabase.rpc('admin_resolve_my_branch_report',{p_report_id:id,p_action:action});if(error)return toast.error(error.message);toast.success(action==='resolved'?'Report resolved':'Report dismissed');void load()}return <Section title="Reports" note="Listing reports and moderation outcomes.">{loading?<Loading/>:rows.length===0?<Empty title="No reports" text="No listing reports are recorded."/>:<div className="space-y-3">{rows.map(r=><Card key={r.id}><Top title={r.reason||'Listing report'} sub={`${r.listing_id||'Unknown listing'} · ${new Date(r.created_at).toLocaleDateString()}`} status={r.status}/>{r.status==='pending'&&<div className="mt-4 flex gap-2"><Btn onClick={()=>void act(r.id,'resolved')}>Resolve</Btn><Btn muted onClick={()=>void act(r.id,'dismissed')}>Dismiss</Btn></div>}</Card>)}</div>}</Section>}
function Settings({profile}:{profile:Profile}){return <div className="space-y-6"><CreatorSettingsTab profile={profile}/><section className="grid gap-5 lg:grid-cols-2"><Card><h3 className="mb-3 text-sm font-semibold">Worker service categories</h3><ServiceCategoryManager profile={profile}/></Card><Card><h3 className="mb-3 text-sm font-semibold">Property types</h3><PropertyTypeManager profile={profile}/></Card></section></div>}
function Section({title,note,right,children}:{title:string;note:string;right?:React.ReactNode;children:React.ReactNode}){return <div className="space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="text-lg font-bold">{title}</h2><p className="mt-1 text-[10px] text-[#707386]">{note}</p></div>{right}</div>{children}</div>}
function Card({children}:{children:React.ReactNode}){return <div className="rounded-2xl border border-white/[0.06] bg-[#10131B] p-4">{children}</div>}
function Grid({children}:{children:React.ReactNode}){return <div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{children}</div>}
function Stats({items}:{items:Array<[string,string|number]>}){return <div className="grid grid-cols-2 gap-2 sm:grid-cols-4">{items.map(([l,v])=><Card key={l}><p className="text-[9px] text-[#666A7C]">{l}</p><p className="mt-2 text-xl font-bold">{v}</p></Card>)}</div>}
function Top({title,sub,status,right}:{title:string;sub:string;status:string;right?:string}){return <div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="truncate text-sm font-semibold">{title}</p><p className="mt-1 text-[10px] leading-relaxed text-[#707386]">{sub}</p></div><div className="shrink-0 text-right">{right&&<p className="mb-1 text-sm font-bold">{right}</p>}<Badge value={status}/></div></div>}
function Badge({value}:{value:string}){const n=String(value||'unknown').toLowerCase();const good=['available','active','approved','completed','resolved','verified','paid'].some(x=>n.includes(x)),bad=['rejected','suspended','failed','cancelled'].some(x=>n.includes(x));return <span className={`rounded-full px-2 py-1 text-[8px] font-semibold capitalize ${good?'bg-emerald-500/10 text-emerald-300':bad?'bg-red-500/10 text-red-300':'bg-amber-500/10 text-amber-300'}`}>{n.replace(/_/g,' ')}</span>}
function Chip({active,onClick,children}:{active:boolean;onClick:()=>void;children:React.ReactNode}){return <button onClick={onClick} className={`rounded-xl px-3 py-2 text-[10px] font-semibold ${active?'bg-violet-500 text-white':'border border-white/[0.06] bg-[#10131B] text-[#777A8C]'}`}>{children}</button>}
function Segment({value,set,items}:{value:string;set:(v:string)=>void;items:string[][]}){return <div className="flex rounded-xl border border-white/[0.06] bg-[#0C0F17] p-1">{items.map(([id,label])=><button key={id} onClick={()=>set(id)} className={`rounded-lg px-3 py-2 text-[9px] font-semibold ${value===id?'bg-violet-500 text-white':'text-[#777A8C]'}`}>{label}</button>)}</div>}
function Btn({children,onClick,muted,danger}:{children:React.ReactNode;onClick:()=>void;muted?:boolean;danger?:boolean}){return <button onClick={onClick} className={`min-h-10 flex-1 rounded-xl px-3 text-[10px] font-semibold ${danger?'border border-red-500/20 bg-red-500/10 text-red-300':muted?'border border-white/[0.08] bg-white/[0.04] text-[#A7A9B6]':'bg-violet-500 text-white'}`}>{children}</button>}
function Field({value,set,placeholder,type='text'}:{value:string;set:(v:string)=>void;placeholder:string;type?:string}){return <input value={value} onChange={e=>set(e.target.value)} placeholder={placeholder} type={type} className="h-10 w-full rounded-xl border border-white/[0.08] bg-[#171A23] px-3 text-xs outline-none focus:border-violet-500/40"/>}
function Avatar({text}:{text:string}){return <div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-gradient-to-br from-violet-500 to-indigo-600 text-sm font-bold">{(text||'W')[0].toUpperCase()}</div>}
function Evidence({title,url}:{title:string;url:string}){return <div><p className="mb-2 text-[9px] text-[#6E7183]">{title}</p><img src={url} alt={title} className="max-h-60 w-full rounded-xl bg-[#161922] object-contain"/></div>}
function Video({title,url}:{title:string;url:string}){return <div><p className="mb-2 text-[9px] text-[#6E7183]">{title}</p><video src={url} controls className="max-h-60 w-full rounded-xl bg-[#161922]"/></div>}
function Missing({label}:{label:string}){return <div className="grid min-h-40 place-items-center rounded-xl border border-dashed border-red-500/20 bg-red-500/[0.03] text-[10px] text-red-300">No {label} uploaded</div>}
function Empty({title,text}:{title:string;text:string}){return <div className="rounded-2xl border border-dashed border-white/[0.08] bg-white/[0.015] px-6 py-14 text-center"><p className="text-sm font-semibold">{title}</p><p className="mx-auto mt-2 max-w-md text-[10px] leading-relaxed text-[#66697B]">{text}</p></div>}
function Loading(){return <div className="grid min-h-40 place-items-center"><div className="h-7 w-7 animate-spin rounded-full border-2 border-violet-500 border-t-transparent"/></div>}
function money(value:any){return `₦${Number(value||0).toLocaleString('en-NG')}`}
