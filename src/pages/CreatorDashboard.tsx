import { useEffect,useMemo,useState } from 'react';
import { Toaster,toast } from 'sonner';
import { supabase } from '@/lib/supabase';
import { useCreatorAuth } from '@/hooks/useCreatorAuth';
import UserProfileModal from '@/components/UserProfileModal';
import CommunicationsWorkspace from '@/components/CommunicationsWorkspace';
import PropertyPipelineWorkspace from '@/components/PropertyPipelineWorkspace';
import StaffListTab from './StaffListTab';
import CreatorSettingsTab from './CreatorSettingsTab';
import AnalyticsPage from './AnalyticsPage';
import DomainSettingsPanel from '@/components/DomainSettingsPanel';
import ServiceCategoryManager from '@/components/ServiceCategoryManager';
import PropertyTypeManager from '@/components/PropertyTypeManager';
import type { Profile } from '@/types';

type Tab='overview'|'operations'|'communications'|'finance'|'analytics'|'settings';
type Operation='people'|'team'|'properties'|'workers'|'bookings'|'issues';
type PersonRole='user'|'property_partner';
type Props={profile:Profile;onLogout:()=>void;onNavigate?:(page:string)=>void;onGoToChat?:(id?:string)=>void};
const TABS:[Tab,string,string][]=[['overview','Overview','Platform health and work that needs attention'],['operations','Operations','Accounts, team, properties, workers, bookings and moderation'],['communications','Communications','Platform inbox and official announcements'],['finance','Finance','Money movement, commission records and financial rules'],['analytics','Analytics','Platform performance and reporting'],['settings','Settings','Global platform and marketplace configuration']];
const OPS:[Operation,string,string][]=[['people','People','Regular users and Property Partners'],['team','Team','Admins, Staff, branch placement and Staff modules'],['properties','Properties','Inspection-led property pipeline across WeHouse'],['workers','Workers','Worker accounts, status and verification review'],['bookings','Bookings','Worker, apartment and hotel booking records'],['issues','Issues','Listing reports and moderation work']];
export default function CreatorDashboard({profile,onLogout,onNavigate,onGoToChat}:Props){const {clearAuth}=useCreatorAuth();const [tab,setTab]=useState<Tab>('overview'),[operation,setOperation]=useState<Operation>('people'),[viewing,setViewing]=useState<Profile|null>(null);const current=TABS.find(x=>x[0]===tab)!;function logout(){clearAuth();onLogout()}function openOperation(next:Operation){setOperation(next);setTab('operations')}return <div className="role-workspace min-h-[100dvh] bg-[#070910] pb-24 text-white"><Toaster position="top-center" richColors/><header className="sticky top-0 z-40 border-b border-white/[0.06] bg-[#080A12]/95 backdrop-blur-xl"><div className="mx-auto max-w-[1500px] px-4 pt-4 sm:px-5 lg:px-8"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-[9px] font-bold tracking-[.25em] text-violet-400">WEHOUSE CREATOR</p><h1 className="mt-1 text-lg font-bold sm:text-xl">{current[1]}</h1><p className="mt-1 max-w-2xl text-[10px] leading-relaxed text-[#74798B]">{current[2]}</p></div><div className="flex shrink-0 gap-2">{onNavigate&&<button onClick={()=>onNavigate('profile')} className="rounded-xl border border-white/[0.08] bg-white/[0.04] px-3 py-2 text-[10px] text-[#A3A6B5]">Account</button>}<button onClick={logout} className="rounded-xl border border-red-500/15 bg-red-500/[0.06] px-3 py-2 text-[10px] text-red-300">Log out</button></div></div><div className="mt-4 overflow-x-auto pb-3 scrollbar-hide"><div className="flex min-w-max gap-1">{TABS.map(([id,label])=><button key={id} onClick={()=>setTab(id)} className={`rounded-xl px-3.5 py-2 text-[10px] font-semibold ${tab===id?'bg-violet-500 text-white':'text-[#74798B] hover:bg-white/[0.04] hover:text-white'}`}>{label}</button>)}</div></div></div></header><main className="mx-auto max-w-[1500px] px-4 py-6 sm:px-5 lg:px-8 lg:py-8">{tab==='overview'&&<Overview openOperation={openOperation} openCommunications={()=>setTab('communications')} openFinance={()=>setTab('finance')} openAnalytics={()=>setTab('analytics')}/>} {tab==='operations'&&<Operations profile={profile} active={operation} setActive={setOperation} onView={setViewing}/>} {tab==='communications'&&<CommunicationsWorkspace profile={profile} scope="all"/>} {tab==='finance'&&<Finance/>} {tab==='analytics'&&<AnalyticsPage profile={profile}/>} {tab==='settings'&&<Settings profile={profile}/>}</main>{viewing&&<UserProfileModal user={viewing} adminProfile={profile} onClose={()=>setViewing(null)} onNavigate={onNavigate} onGoToChat={onGoToChat}/>}</div>}
function Overview({openOperation,openCommunications,openFinance,openAnalytics}:{openOperation:(t:Operation)=>void;openCommunications:()=>void;openFinance:()=>void;openAnalytics:()=>void}){const [stats,setStats]=useState<any>({users:0,workers:0,partners:0,staff:0,listings:0,pending_verifications:0,hotels:0,pendingWithdrawals:0,pendingInspections:0}),[loading,setLoading]=useState(true);useEffect(()=>{void(async()=>{const [base,hotels,withdrawals,inspections]=await Promise.all([supabase.rpc('admin_get_my_branch_stats'),supabase.from('hotels').select('*',{count:'exact',head:true}).eq('status','active'),supabase.from('withdrawals').select('*',{count:'exact',head:true}).in('status',['pending','processing']),supabase.from('inspection_requests').select('*',{count:'exact',head:true}).in('status',['pending','scheduled','in_progress'])]);if(base.error)toast.error(base.error.message);setStats({...((base.data||{}) as any),hotels:hotels.count||0,pendingWithdrawals:withdrawals.count||0,pendingInspections:inspections.count||0});setLoading(false)})()},[]);if(loading)return <Loading/>;const cards:[string,number,()=>void,string][]=[['Users',stats.users,()=>openOperation('people'),'Customer accounts'],['Property Partners',stats.partners,()=>openOperation('people'),'Property owners'],['Team',stats.staff,()=>openOperation('team'),'Admins and Staff'],['Workers',stats.workers,()=>openOperation('workers'),`${stats.pending_verifications||0} awaiting review`],['Published apartments',stats.listings,()=>openOperation('properties'),'Inspection-linked listings'],['Published hotels',stats.hotels,()=>openOperation('properties'),'Inspection-linked hotel inventory'],['Active inspections',stats.pendingInspections,()=>openOperation('properties'),'Property field work'],['Withdrawals',stats.pendingWithdrawals,openFinance,'Pending finance work']];return <div className="space-y-6"><section className="overflow-hidden rounded-3xl border border-violet-500/15 bg-gradient-to-br from-violet-500/[0.14] via-[#14111F] to-[#0E1118] p-5 sm:p-6 lg:p-8"><span className="rounded-full bg-violet-500/10 px-3 py-1 text-[9px] font-semibold text-violet-300">PLATFORM COMMAND CENTER</span><h2 className="mt-4 max-w-3xl text-2xl font-bold sm:text-3xl lg:text-4xl">Global control without duplicate workflows.</h2><p className="mt-3 max-w-2xl text-xs leading-relaxed text-[#9498A9]">Properties follow one inspection pipeline nationwide. Team controls branch staffing. Communications, Finance, Analytics and Settings remain separate responsibilities.</p><div className="mt-5 flex flex-wrap gap-2"><button onClick={()=>openOperation('properties')} className="rounded-xl bg-violet-500 px-4 py-3 text-xs font-semibold">Open Property Pipeline</button><button onClick={openCommunications} className="rounded-xl border border-white/[0.08] bg-white/[0.04] px-4 py-3 text-xs font-semibold">Communications</button><button onClick={openFinance} className="rounded-xl border border-white/[0.08] bg-white/[0.04] px-4 py-3 text-xs font-semibold">Finance</button><button onClick={openAnalytics} className="rounded-xl border border-white/[0.08] bg-white/[0.04] px-4 py-3 text-xs font-semibold">Analytics</button></div></section><section className="grid grid-cols-2 gap-3 md:grid-cols-3 xl:grid-cols-4">{cards.map(([label,value,action,note])=><button key={label} onClick={action} className="rounded-2xl border border-white/[0.06] bg-[#10131B] p-4 text-left hover:border-violet-500/25"><p className="text-2xl font-bold">{value||0}</p><p className="mt-1 text-[10px] font-semibold">{label}</p><p className="mt-1 text-[9px] text-[#616678]">{note}</p></button>)}</section></div>}
function Operations({profile,active,setActive,onView}:{profile:Profile;active:Operation;setActive:(t:Operation)=>void;onView:(p:Profile)=>void}){const current=OPS.find(x=>x[0]===active)!;return <div className="space-y-5"><div><h2 className="text-lg font-bold">Platform operations</h2><p className="mt-1 text-[10px] text-[#707386]">Every operational responsibility has one canonical home.</p></div><div className="overflow-x-auto scrollbar-hide"><div className="flex min-w-max gap-1 rounded-2xl border border-white/[0.05] bg-[#0D1017] p-1">{OPS.map(([id,label])=><button key={id} onClick={()=>setActive(id)} className={`rounded-xl px-3 py-2 text-[10px] font-semibold ${active===id?'bg-violet-500 text-white':'text-[#777A8C]'}`}>{label}</button>)}</div></div><div className="rounded-2xl border border-violet-500/10 bg-violet-500/[0.03] p-3"><p className="text-xs font-semibold">{current[1]}</p><p className="mt-1 text-[9px] text-[#6E7183]">{current[2]}</p></div>{active==='people'&&<People onView={onView}/>} {active==='team'&&<StaffListTab profile={profile}/>} {active==='properties'&&<PropertyPipelineWorkspace profile={profile}/>} {active==='workers'&&<Workers/>} {active==='bookings'&&<Bookings/>} {active==='issues'&&<Issues/>}</div>}
function People({onView}:{onView:(p:Profile)=>void}){const [role,setRole]=useState<PersonRole>('user'),[rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true),[search,setSearch]=useState('');useEffect(()=>{void load()},[role]);async function load(){setLoading(true);const {data,error}=await supabase.rpc('admin_get_my_branch_profiles',{p_role:role});if(error)toast.error(error.message);setRows(Array.isArray(data)?data:[]);setLoading(false)}const shown=useMemo(()=>rows.filter(r=>!search.trim()||[r.full_name,r.username,r.email,r.user_id].filter(Boolean).join(' ').toLowerCase().includes(search.toLowerCase())),[rows,search]);return <Section title="People" note="Workers and the WeHouse Team have dedicated operational areas."><div className="flex gap-2">{([['user','Users'],['property_partner','Property Partners']] as const).map(([id,label])=><Chip key={id} active={role===id} onClick={()=>setRole(id)}>{label}</Chip>)}</div><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search platform accounts" className="h-11 w-full rounded-xl border border-white/[0.08] bg-[#141720] px-3 text-xs"/>{loading?<Loading/>:shown.length===0?<Empty title="No matching accounts" text="No accounts match this filter."/>:<Grid>{shown.map(p=><button key={p.user_id} onClick={()=>onView(p)} className="rounded-2xl border border-white/[0.06] bg-[#10131B] p-4 text-left"><div className="flex gap-3"><Avatar text={p.full_name||p.username||p.email}/><div className="min-w-0"><p className="truncate text-sm font-semibold">{p.full_name||p.username||'WeHouse account'}</p><p className="truncate text-[10px] text-[#6D7082]">{p.email}</p><p className="mt-1 text-[9px] capitalize text-[#535667]">{p.role?.replace(/_/g,' ')} · {[p.local_government||p.city,p.state].filter(Boolean).join(', ')||'Location not set'}</p></div></div></button>)}</Grid>}</Section>}
function Workers(){const [rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true),[filter,setFilter]=useState('all'),[selected,setSelected]=useState<any|null>(null),[reason,setReason]=useState('');async function load(){setLoading(true);const {data,error}=await supabase.rpc('admin_get_my_branch_profiles',{p_role:'worker'});if(error)toast.error(error.message);setRows(Array.isArray(data)?data:[]);setLoading(false)}useEffect(()=>{void load()},[]);const shown=filter==='all'?rows:rows.filter(w=>w.worker_status===filter);async function review(id:string,decision:'approve'|'reject'){if(decision==='reject'&&!reason.trim())return toast.error('Enter a rejection reason');const {error}=await supabase.rpc('admin_review_my_branch_worker',{p_worker_id:id,p_decision:decision,p_reason:decision==='reject'?reason.trim():null});if(error)return toast.error(error.message);toast.success(decision==='approve'?'Worker verified':'Worker rejected');setSelected(null);setReason('');void load()}if(selected)return <Section title="Worker" note="Identity, service information and verification evidence."><button onClick={()=>{setSelected(null);setReason('')}} className="text-[10px] font-semibold text-violet-400">← Back to workers</button><Card><Top title={selected.full_name||selected.username||'Worker'} sub={`${selected.worker_occupation||'Occupation not set'} · ${[selected.local_government||selected.city,selected.state].filter(Boolean).join(', ')}`} status={selected.worker_status}/><div className="mt-4 grid gap-3 md:grid-cols-2">{selected.worker_gov_id_url?<Evidence title="Government ID" url={selected.worker_gov_id_url}/>:<Missing label="Government ID"/>}{selected.worker_video_url?<Video title="Skill video" url={selected.worker_video_url}/>:<Missing label="Skill video"/>}</div>{selected.worker_status==='profile_under_review'&&<div className="mt-4 space-y-2"><input value={reason} onChange={e=>setReason(e.target.value)} placeholder="Rejection reason if rejecting" className="h-10 w-full rounded-xl border border-white/[0.08] bg-[#171A23] px-3 text-xs"/><div className="flex gap-2"><Btn onClick={()=>void review(selected.user_id,'approve')}>Verify worker</Btn><Btn danger onClick={()=>void review(selected.user_id,'reject')}>Reject</Btn></div></div>}</Card></Section>;return <Section title="Workers" note="Single home for worker accounts and verification review."><div className="flex gap-2 overflow-x-auto pb-1 scrollbar-hide">{[['all','All'],['verification_paid','Verification paid'],['profile_under_review','Under review'],['verified','Verified'],['rejected','Rejected'],['suspended','Suspended']].map(([id,label])=><Chip key={id} active={filter===id} onClick={()=>setFilter(id)}>{label}</Chip>)}</div>{loading?<Loading/>:shown.length===0?<Empty title="No workers" text="No workers match this status."/>:<Grid>{shown.map(w=><button key={w.user_id} onClick={()=>setSelected(w)} className="rounded-2xl border border-white/[0.06] bg-[#10131B] p-4 text-left"><Top title={w.full_name||w.username||'Worker'} sub={`${w.worker_occupation||'Occupation not set'} · ${[w.local_government||w.city,w.state].filter(Boolean).join(', ')}`} status={w.worker_status}/><p className="mt-3 text-[9px] font-semibold text-violet-400">OPEN WORKER →</p></button>)}</Grid>}</Section>}
function Bookings(){const [view,setView]=useState<'worker'|'apartments'|'hotels'>('worker'),[rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{void load()},[view]);async function load(){setLoading(true);let data:any[]=[],error:any=null;if(view==='worker'){const r=await supabase.rpc('admin_get_my_branch_worker_bookings');data=Array.isArray(r.data)?r.data:[];error=r.error}else if(view==='apartments'){const r=await supabase.from('reservations').select('*').order('created_at',{ascending:false}).limit(100);data=r.data||[];error=r.error}else{const r=await supabase.from('hotel_bookings').select('*,hotels(name,city,state),hotel_rooms(room_type)').order('created_at',{ascending:false}).limit(100);data=r.data||[];error=r.error}if(error)toast.error(error.message);setRows(data);setLoading(false)}return <Section title="Bookings" note="Global monitoring across the implemented booking systems." right={<Segment value={view} set={v=>setView(v as any)} items={[['worker','Worker services'],['apartments','Apartments'],['hotels','Hotels']]}/>}>{loading?<Loading/>:rows.length===0?<Empty title="No bookings" text="No records were found for this booking type."/>:<div className="space-y-3">{rows.map(r=><Card key={r.id||r.booking_id}><Top title={view==='worker'?(r.service_type||r.booking_code||'Worker booking'):view==='hotels'?(r.hotels?.name||'Hotel booking'):(r.reservation_code||r.listing_id||'Apartment reservation')} sub={dateText(r.created_at)} status={r.status||'recorded'} right={money(r.negotiated_amount||r.agreed_amount||r.total_price||r.amount||0)}/></Card>)}</div>}</Section>}
function Issues(){return <Section title="Issues" note="Moderation and exception work only. Conversations belong in Communications."><Reports/></Section>}
function Reports(){const [rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true);async function load(){setLoading(true);const {data,error}=await supabase.rpc('admin_get_my_branch_reports');if(error)toast.error(error.message);setRows(Array.isArray(data)?data:[]);setLoading(false)}useEffect(()=>{void load()},[]);async function act(id:string,action:'resolved'|'dismissed'){const {error}=await supabase.rpc('admin_resolve_my_branch_report',{p_report_id:id,p_action:action});if(error)return toast.error(error.message);toast.success(action==='resolved'?'Report resolved':'Report dismissed');void load()}return loading?<Loading/>:rows.length===0?<Empty title="No reports" text="No listing reports are recorded."/>:<div className="space-y-3">{rows.map(r=><Card key={r.id}><Top title={r.reason||'Listing report'} sub={`${r.listing_id||'Unknown listing'} · ${dateText(r.created_at)}`} status={r.status||'pending'}/>{r.status==='pending'&&<div className="mt-4 flex gap-2"><Btn onClick={()=>void act(r.id,'resolved')}>Resolve</Btn><Btn muted onClick={()=>void act(r.id,'dismissed')}>Dismiss</Btn></div>}</Card>)}</div>}
function Finance(){const [view,setView]=useState<'withdrawals'|'commissions'>('withdrawals'),[rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{void load()},[view]);async function load(){setLoading(true);const query=view==='withdrawals'?supabase.from('withdrawals').select('*').order('created_at',{ascending:false}).limit(100):supabase.from('commission_ledger').select('*').order('created_at',{ascending:false}).limit(100);const {data,error}=await query;if(error)toast.error(error.message);setRows(data||[]);setLoading(false)}const total=rows.reduce((s,r)=>s+Number(view==='withdrawals'?r.amount:r.commission_amount||0),0);return <Section title="Finance" note="Financial records and the exact rules used by settlement workflows." right={<Segment value={view} set={v=>setView(v as any)} items={[['withdrawals','Withdrawals'],['commissions','Commissions']]}/> }><div className="grid gap-5 xl:grid-cols-[minmax(0,1.4fr)_minmax(320px,.8fr)]"><div className="space-y-4"><Stats items={[['Records',rows.length],['Value',money(total)]]}/>{loading?<Loading/>:rows.length===0?<Empty title="No finance records" text="No records are available."/>:<div className="space-y-2">{rows.map(r=><Card key={r.id}><Top title={view==='withdrawals'?(r.snapshot_bank_account_name||'Withdrawal'):(r.booking_type||'Commission')} sub={dateText(r.created_at)} status={r.status||'recorded'} right={money(view==='withdrawals'?r.amount:r.commission_amount)}/></Card>)}</div>}</div><DomainSettingsPanel title="Financial rules" description="Canonical settings used by Worker and Property Partner settlement." settings={[{key:'worker_commission_rate',label:'Worker commission (%)',description:'Commission retained from completed worker jobs.',type:'number',defaultValue:'10'},{key:'commission_apartment',label:'Apartment partner commission (%)',description:'Commission on eligible apartment payments.',type:'number',defaultValue:'10'},{key:'commission_hotel',label:'Hotel partner commission (%)',description:'Commission on eligible hotel payments.',type:'number',defaultValue:'10'},{key:'wallet_minimum_withdrawal',label:'Worker minimum withdrawal (₦)',description:'Minimum worker balance required.',type:'number',defaultValue:'1000'},{key:'min_withdrawal',label:'Property Partner minimum withdrawal (₦)',description:'Minimum partner balance required.',type:'number',defaultValue:'5000'}]}/></div></Section>}
function Settings({profile}:{profile:Profile}){return <div className="space-y-7"><CreatorSettingsTab profile={profile}/><section className="space-y-4"><div><h2 className="text-base font-bold">Marketplace configuration</h2><p className="mt-1 text-[10px] text-[#686C7E]">Global choices used by marketplace workflows; not operational records.</p></div><div className="grid gap-5 xl:grid-cols-2"><Card><h3 className="mb-3 text-sm font-semibold">Worker service categories</h3><ServiceCategoryManager profile={profile}/></Card><Card><h3 className="mb-3 text-sm font-semibold">Property types</h3><PropertyTypeManager profile={profile}/></Card></div></section></div>}
function Section({title,note,right,children}:{title:string;note:string;right?:React.ReactNode;children:React.ReactNode}){return <div className="space-y-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="text-lg font-bold">{title}</h2><p className="mt-1 text-[10px] text-[#707386]">{note}</p></div>{right}</div>{children}</div>}function Card({children}:{children:React.ReactNode}){return <div className="rounded-2xl border border-white/[0.06] bg-[#10131B] p-4">{children}</div>}function Grid({children}:{children:React.ReactNode}){return <div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{children}</div>}function Stats({items}:{items:Array<[string,string|number]>}){return <div className="grid grid-cols-2 gap-2 sm:grid-cols-4">{items.map(([l,v])=><Card key={l}><p className="text-[9px] text-[#666A7C]">{l}</p><p className="mt-2 text-xl font-bold">{v}</p></Card>)}</div>}function Top({title,sub,status,right}:{title:string;sub:string;status:string;right?:string}){return <div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="truncate text-sm font-semibold">{title}</p><p className="mt-1 line-clamp-2 text-[10px] text-[#707386]">{sub}</p></div><div className="shrink-0 text-right">{right&&<p className="mb-1 text-sm font-bold">{right}</p>}<Badge value={status}/></div></div>}function Badge({value}:{value:string}){const t=String(value||'unknown').toLowerCase(),good=['available','active','approved','completed','resolved','verified','paid'].some(x=>t.includes(x)),bad=['rejected','suspended','failed','cancelled'].some(x=>t.includes(x));return <span className={`rounded-full px-2 py-1 text-[8px] font-semibold capitalize ${good?'bg-emerald-500/10 text-emerald-300':bad?'bg-red-500/10 text-red-300':'bg-amber-500/10 text-amber-300'}`}>{t.replace(/_/g,' ')}</span>}function Chip({active,onClick,children}:{active:boolean;onClick:()=>void;children:React.ReactNode}){return <button onClick={onClick} className={`shrink-0 rounded-xl px-3 py-2 text-[10px] font-semibold ${active?'bg-violet-500 text-white':'border border-white/[0.06] bg-[#10131B] text-[#777A8C]'}`}>{children}</button>}function Segment({value,set,items}:{value:string;set:(v:string)=>void;items:string[][]}){return <div className="max-w-full overflow-x-auto scrollbar-hide"><div className="flex min-w-max rounded-xl border border-white/[0.06] bg-[#0C0F17] p-1">{items.map(([id,label])=><button key={id} onClick={()=>set(id)} className={`rounded-lg px-3 py-2 text-[9px] font-semibold ${value===id?'bg-violet-500 text-white':'text-[#777A8C]'}`}>{label}</button>)}</div></div>}function Btn({children,onClick,muted,danger}:{children:React.ReactNode;onClick:()=>void;muted?:boolean;danger?:boolean}){return <button onClick={onClick} className={`min-h-10 flex-1 rounded-xl px-3 text-[10px] font-semibold ${danger?'border border-red-500/20 bg-red-500/10 text-red-300':muted?'border border-white/[0.08] bg-white/[0.04] text-[#A7A9B6]':'bg-violet-500 text-white'}`}>{children}</button>}function Avatar({text}:{text:string}){return <div className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-gradient-to-br from-violet-500 to-indigo-600 text-sm font-bold">{(text||'W')[0].toUpperCase()}</div>}function Evidence({title,url}:{title:string;url:string}){return <div><p className="mb-2 text-[9px] text-[#6E7183]">{title}</p><img src={url} alt={title} className="max-h-60 w-full rounded-xl bg-[#161922] object-contain"/></div>}function Video({title,url}:{title:string;url:string}){return <div><p className="mb-2 text-[9px] text-[#6E7183]">{title}</p><video src={url} controls className="max-h-60 w-full rounded-xl bg-[#161922]"/></div>}function Missing({label}:{label:string}){return <div className="grid min-h-40 place-items-center rounded-xl border border-dashed border-red-500/20 bg-red-500/[0.03] text-[10px] text-red-300">No {label} uploaded</div>}function Empty({title,text}:{title:string;text:string}){return <div className="rounded-2xl border border-dashed border-white/[0.08] bg-white/[0.015] px-6 py-12 text-center"><p className="text-sm font-semibold">{title}</p><p className="mx-auto mt-2 max-w-md text-[10px] text-[#66697B]">{text}</p></div>}function Loading(){return <div className="grid min-h-40 place-items-center"><div className="h-7 w-7 animate-spin rounded-full border-2 border-violet-500 border-t-transparent"/></div>}function money(v:any){return `₦${Number(v||0).toLocaleString('en-NG')}`}function dateText(v:any){if(!v)return'Date unavailable';try{return new Date(v).toLocaleString()}catch{return String(v)}}
