import { useEffect,useMemo,useRef,useState } from 'react';
import { checkUsernameAvailable,removeAvatar,updateProfile,uploadAvatar,validateUsername } from '@/lib/supabase';
import { NIGERIA_STATES } from '@/data/nigeria-locations';
import { Toaster,toast } from 'sonner';
import type { Profile } from '@/types';

type Props={profile:Profile;onUpdate:(p:Profile)=>void;onBack:()=>void};
export default function ProfileEdit({profile,onUpdate,onBack}:Props){
  const fileRef=useRef<HTMLInputElement>(null);
  const [saving,setSaving]=useState(false);const [uploading,setUploading]=useState(false);
  const [avatar,setAvatar]=useState(profile.avatar_url||'');
  const [username,setUsername]=useState(profile.username||'');
  const [usernameState,setUsernameState]=useState<'idle'|'checking'|'available'|'taken'|'invalid'>('idle');
  const [fullName,setFullName]=useState(profile.full_name||'');const [phone,setPhone]=useState(profile.phone||'');const [bio,setBio]=useState(profile.bio||'');
  const [gender,setGender]=useState(profile.gender||'');const [isStudent,setIsStudent]=useState(Boolean(profile.is_student));const [school,setSchool]=useState(profile.school||'');
  const [state,setState]=useState(profile.state||'');const [lga,setLga]=useState(profile.local_government||profile.city||'');const [area,setArea]=useState(profile.area||'');
  const isUser=profile.role==='user';
  const states=useMemo(()=>NIGERIA_STATES.map(x=>x.state).sort(),[]);
  const lgas=useMemo(()=>NIGERIA_STATES.find(x=>x.state===state)?.cities||[],[state]);

  useEffect(()=>{const value=username.trim().toLowerCase();if(!value||value===(profile.username||'').toLowerCase()){setUsernameState('idle');return}const valid=validateUsername(value);if(!valid.valid){setUsernameState('invalid');return}setUsernameState('checking');const timer=window.setTimeout(async()=>{const {available}=await checkUsernameAvailable(value,profile.user_id);setUsernameState(available?'available':'taken')},350);return()=>window.clearTimeout(timer)},[username,profile.username,profile.user_id]);

  async function changePhoto(e:React.ChangeEvent<HTMLInputElement>){const file=e.target.files?.[0];if(!file)return;if(!['image/jpeg','image/png','image/webp'].includes(file.type))return toast.error('Choose a JPG, PNG or WebP image');if(file.size>35*1024*1024)return toast.error('Image must be under 35MB');setUploading(true);const {url,error}=await uploadAvatar(file,profile.user_id);setUploading(false);if(error||!url)return toast.error(error?.message||'Photo upload failed');const {profile:updated,error:updateError}=await updateProfile(profile.user_id,{avatar_url:url});if(updateError||!updated)return toast.error(updateError?.message||'Could not save photo');setAvatar(url);onUpdate(updated);toast.success('Profile photo updated')}
  async function deletePhoto(){const {error}=await removeAvatar(profile.user_id);if(error)return toast.error(error.message);const {profile:updated}=await updateProfile(profile.user_id,{avatar_url:null});setAvatar('');if(updated)onUpdate(updated);toast.success('Profile photo removed')}
  async function save(e:React.FormEvent){e.preventDefault();if(usernameState==='taken'||usernameState==='invalid')return toast.error('Fix the username before saving');if(isUser&&!gender)return toast.error('Gender is required for roommate matching');if(isUser&&(!state||!lga))return toast.error('State and Local Government are required');setSaving(true);const updates:any={full_name:fullName.trim()||null,username:username.trim().toLowerCase(),phone:phone.trim()||null};if(isUser){updates.bio=bio.trim()||null;updates.gender=gender;updates.is_student=isStudent;updates.school=isStudent?(school.trim()||null):null;updates.country='Nigeria';updates.state=state;updates.local_government=lga;updates.city=lga;updates.area=area.trim()||null;updates.preferred_location=[lga,state].filter(Boolean).join(', ');updates.profile_complete=true}const {profile:updated,error}=await updateProfile(profile.user_id,updates);setSaving(false);if(error||!updated)return toast.error(error?.message||'Could not save profile');onUpdate(updated);toast.success('Profile updated')}

  return <div className="min-h-[100dvh] bg-[#09090D] pb-24 text-white"><Toaster position="top-center" richColors/><header className="border-b border-white/[0.06] bg-[#0D0E14] px-4 py-4 sm:px-5"><div className="mx-auto flex max-w-3xl items-center gap-3"><button onClick={onBack} className="text-[#8A8D9D] hover:text-white">←</button><div><h1 className="text-base font-semibold">Profile</h1><p className="mt-0.5 text-[10px] text-[#626678]">Personal account information only.</p></div></div></header><form onSubmit={save} className="mx-auto max-w-3xl space-y-5 px-4 py-5 sm:px-5"><section className="rounded-2xl border border-white/[0.06] bg-[#11131B] p-4 sm:p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><button type="button" onClick={()=>fileRef.current?.click()} className="grid h-20 w-20 shrink-0 place-items-center overflow-hidden rounded-2xl bg-blue-500/15 text-xl font-bold text-blue-300">{avatar?<img src={avatar} alt="Profile" className="h-full w-full object-cover"/>:(username||'U')[0].toUpperCase()}</button><input ref={fileRef} type="file" accept="image/jpeg,image/png,image/webp" onChange={changePhoto} className="hidden"/><div className="flex-1"><p className="text-sm font-semibold">Profile photo</p><p className="mt-1 text-[10px] text-[#686C7D]">{uploading?'Uploading…':'JPG, PNG or WebP'}</p><div className="mt-3 flex gap-2"><button type="button" onClick={()=>fileRef.current?.click()} className="rounded-xl border border-white/[0.08] px-3 py-2 text-[10px] font-semibold">{avatar?'Change photo':'Add photo'}</button>{avatar&&<button type="button" onClick={()=>void deletePhoto()} className="rounded-xl border border-red-500/15 px-3 py-2 text-[10px] font-semibold text-red-300">Remove</button>}</div></div></div></section><section className="rounded-2xl border border-white/[0.06] bg-[#11131B] p-4 sm:p-5"><div className="grid gap-4 sm:grid-cols-2"><Field label="Full name" value={fullName} onChange={setFullName}/><div><Field label="Username" value={username} onChange={setUsername}/><p className={`mt-1 text-[9px] ${usernameState==='available'?'text-emerald-300':usernameState==='taken'||usernameState==='invalid'?'text-red-300':'text-[#626678]'}`}>{usernameState==='checking'?'Checking…':usernameState==='available'?'Username available':usernameState==='taken'?'Username already taken':usernameState==='invalid'?'Use 3–20 letters, numbers or underscores':''}</p></div><Field label="Phone" value={phone} onChange={setPhone}/></div>{!isUser&&<div className="mt-4 rounded-xl border border-blue-500/15 bg-blue-500/[0.05] p-3 text-[10px] leading-relaxed text-[#8EA6C7]">Role, branch assignment and professional/service information are managed in your operational workspace, not here.</div>}</section>{isUser&&<><section className="rounded-2xl border border-white/[0.06] bg-[#11131B] p-4 sm:p-5"><h2 className="text-sm font-semibold">About you</h2><div className="mt-4 space-y-4"><label className="block"><span className="mb-1 block text-[10px] text-[#777B8D]">Bio</span><textarea value={bio} onChange={e=>setBio(e.target.value)} rows={3} className="w-full resize-none rounded-xl border border-white/[0.08] bg-[#181A23] p-3 text-xs outline-none focus:border-blue-500/40"/></label><div><p className="mb-2 text-[10px] text-[#777B8D]">Gender</p><div className="flex gap-2">{[['male','Male'],['female','Female']].map(([id,label])=><button type="button" key={id} onClick={()=>setGender(id)} className={`rounded-xl px-4 py-2 text-[10px] font-semibold ${gender===id?'bg-blue-500':'border border-white/[0.08] text-[#A0A4B3]'}`}>{label}</button>)}</div></div><label className="flex items-center justify-between gap-4 rounded-xl border border-white/[0.06] bg-white/[0.02] p-3"><div><p className="text-xs font-medium">Student</p><p className="mt-1 text-[9px] text-[#626678]">Add your institution for roommate compatibility.</p></div><button type="button" onClick={()=>setIsStudent(v=>!v)} className={`relative h-6 w-11 rounded-full ${isStudent?'bg-blue-500':'bg-[#2A2D38]'}`}><span className={`absolute left-0.5 top-0.5 h-5 w-5 rounded-full bg-white transition-transform ${isStudent?'translate-x-5':''}`}/></button></label>{isStudent&&<Field label="Institution" value={school} onChange={setSchool}/>}</div></section><section className="rounded-2xl border border-white/[0.06] bg-[#11131B] p-4 sm:p-5"><h2 className="text-sm font-semibold">Location</h2><p className="mt-1 text-[10px] text-[#686C7D]">This State/LGA is the authority used by roommate matching and local WeHouse discovery.</p><div className="mt-4 grid gap-4 sm:grid-cols-2"><Select label="State" value={state} onChange={v=>{setState(v);setLga('')}} options={states}/><Select label="Local Government" value={lga} onChange={setLga} options={lgas} disabled={!state}/><div className="sm:col-span-2"><Field label="Area / neighbourhood (optional)" value={area} onChange={setArea}/></div></div></section></>}<button type="submit" disabled={saving||usernameState==='checking'} className="w-full rounded-xl bg-blue-500 px-4 py-3 text-xs font-semibold disabled:opacity-50">{saving?'Saving…':'Save profile'}</button></form></div>
}
function Field({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label className="block"><span className="mb-1 block text-[10px] text-[#777B8D]">{label}</span><input value={value} onChange={e=>onChange(e.target.value)} className="h-11 w-full rounded-xl border border-white/[0.08] bg-[#181A23] px-3 text-xs outline-none focus:border-blue-500/40"/></label>}
function Select({label,value,onChange,options,disabled}:{label:string;value:string;onChange:(v:string)=>void;options:string[];disabled?:boolean}){return <label className="block"><span className="mb-1 block text-[10px] text-[#777B8D]">{label}</span><select value={value} onChange={e=>onChange(e.target.value)} disabled={disabled} className="h-11 w-full rounded-xl border border-white/[0.08] bg-[#181A23] px-3 text-xs outline-none disabled:opacity-40"><option value="">Select…</option>{options.map(x=><option key={x} value={x}>{x}</option>)}</select></label>}
