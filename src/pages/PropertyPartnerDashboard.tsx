// Canonical role entrypoint. The implementation remains in the legacy-named
// file temporarily so the route rename can land without duplicating UI logic.
export { default } from './PropertyOwnerDashboard';
