import { useCallback,useEffect,useMemo,useState } from 'react';
import { changePassword,getSessionHistory,logPasswordChange,parseDeviceInfo,supabase } from '@/lib/supabase';
import { Toaster,toast } from 'sonner';
import type { Profile } from '@/types';

type Props={profile:Profile;onBack:()=>void};
export default function SecuritySettings({profile,onBack}:Props){
  const [emailVerified,setEmailVerified]=useState<boolean|null>(null);
  const [history,setHistory]=useState<any[]>([]);
  const [loading,setLoading]=useState(true);
  const [showPassword,setShowPassword]=useState(false);
  const [currentPassword,setCurrentPassword]=useState('');
  const [newPassword,setNewPassword]=useState('');
  const [confirmPassword,setConfirmPassword]=useState('');
  const [changing,setChanging]=useState(false);
  const [deleteText,setDeleteText]=useState('');
  const [deleting,setDeleting]=useState(false);
  const canDelete=['user','worker','property_partner'].includes(profile.role);
  const device=useMemo(()=>parseDeviceInfo(),[]);

  const load=useCallback(async()=>{setLoading(true);const [{data:{user}},{sessions}]=await Promise.all([supabase.auth.getUser(),getSessionHistory(profile.user_id,30)]);setEmailVerified(Boolean(user?.email_confirmed_at));setHistory((sessions||[]).filter((x:any)=>x.action_type==='session_start').slice(0,10));setLoading(false)},[profile.user_id]);
  useEffect(()=>{void load()},[load]);

  async function savePassword(){if(!currentPassword||!newPassword||!confirmPassword)return toast.error('Complete all password fields');if(newPassword!==confirmPassword)return toast.error('New passwords do not match');if(newPassword.length<8)return toast.error('Password must be at least 8 characters');setChanging(true);const {error}=await changePassword(currentPassword,newPassword,profile.email);setChanging(false);if(error)return toast.error(error.message);await logPasswordChange(profile.user_id,profile.auth_id);setCurrentPassword('');setNewPassword('');setConfirmPassword('');setShowPassword(false);toast.success('Password changed')}
  async function logoutAll(){await supabase.auth.signOut({scope:'global'});window.location.reload()}
  async function closeAccount(){if(deleteText!=='DELETE')return;setDeleting(true);const {error}=await supabase.rpc('delete_user_account',{p_user_id:profile.user_id});setDeleting(false);if(error)return toast.error(error.message||'Account could not be closed');await supabase.auth.signOut({scope:'global'});window.location.reload()}

  return <div className="min-h-[100dvh] bg-[#09090D] pb-24 text-white"><Toaster position="top-center" richColors/><header className="border-b border-white/[0.06] bg-[#0D0E14] px-4 py-4 sm:px-5"><div className="mx-auto flex max-w-3xl items-center gap-3"><button onClick={onBack} className="text-[#8A8D9D] hover:text-white">←</button><div><h1 className="text-base font-semibold">Security</h1><p className="mt-0.5 text-[10px] text-[#626678]">Password and session protection for this account.</p></div></div></header><main className="mx-auto max-w-3xl space-y-5 px-4 py-5 sm:px-5"><section className="grid gap-3 sm:grid-cols-3"><Info label="Email" value={emailVerified===null?'Checking…':emailVerified?'Verified':'Not verified'}/><Info label="Current device" value={`${device.device} · ${device.browser}`}/><Info label="Account created" value={new Date(profile.created_at).toLocaleDateString()}/></section><section className="rounded-2xl border border-white/[0.06] bg-[#11131B] p-4 sm:p-5"><div className="flex items-center justify-between gap-3"><div><h2 className="text-sm font-semibold">Password</h2><p className="mt-1 text-[10px] text-[#686C7D]">Change the password used for email sign-in.</p></div><button onClick={()=>setShowPassword(v=>!v)} className="rounded-xl border border-white/[0.08] px-3 py-2 text-[10px] font-semibold text-[#AEB1BF]">{showPassword?'Cancel':'Change password'}</button></div>{showPassword&&<div className="mt-4 space-y-3"><Field label="Current password" value={currentPassword} onChange={setCurrentPassword}/><Field label="New password" value={newPassword} onChange={setNewPassword}/><Field label="Confirm new password" value={confirmPassword} onChange={setConfirmPassword}/><button onClick={()=>void savePassword()} disabled={changing} className="w-full rounded-xl bg-blue-500 px-4 py-3 text-xs font-semibold disabled:opacity-50">{changing?'Changing…':'Save new password'}</button></div>}</section><section className="rounded-2xl border border-white/[0.06] bg-[#11131B] p-4 sm:p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-sm font-semibold">Sessions</h2><p className="mt-1 text-[10px] text-[#686C7D]">Recent sign-ins are shown for awareness. WeHouse does not pretend historical entries can be remotely terminated individually.</p></div><button onClick={()=>void logoutAll()} className="rounded-xl border border-red-500/15 bg-red-500/[0.05] px-3 py-2 text-[10px] font-semibold text-red-300">Log out all devices</button></div><div className="mt-4 space-y-2"><div className="rounded-xl border border-emerald-500/15 bg-emerald-500/[0.05] p-3"><p className="text-xs font-medium text-emerald-300">This device</p><p className="mt-1 text-[10px] text-[#777B8D]">{device.device} · {device.os} · {device.browser}</p></div>{loading?<p className="py-4 text-center text-[10px] text-[#696D7D]">Loading recent sign-ins…</p>:history.length===0?<p className="py-4 text-center text-[10px] text-[#696D7D]">No additional sign-in history.</p>:history.map((entry:any)=><div key={entry.id} className="rounded-xl border border-white/[0.05] bg-white/[0.02] p-3"><p className="text-xs text-[#D2D4DD]">{entry.details?.device||'Device'} · {entry.details?.browser||'Browser'}</p><p className="mt-1 text-[9px] text-[#626678]">{new Date(entry.created_at).toLocaleString()}</p></div>)}</div></section>{canDelete&&<section className="rounded-2xl border border-red-500/15 bg-red-500/[0.04] p-4 sm:p-5"><h2 className="text-sm font-semibold text-red-300">Close account</h2><p className="mt-1 text-[10px] leading-relaxed text-[#8C7077]">The server checks active bookings, balances and other obligations before allowing account closure.</p><div className="mt-4 space-y-3"><input value={deleteText} onChange={e=>setDeleteText(e.target.value)} placeholder="Type DELETE" className="h-11 w-full rounded-xl border border-red-500/15 bg-[#181319] px-3 text-xs outline-none"/><button onClick={()=>void closeAccount()} disabled={deleteText!=='DELETE'||deleting} className="w-full rounded-xl bg-red-500 px-4 py-3 text-xs font-semibold disabled:opacity-40">{deleting?'Closing…':'Close account'}</button></div></section>}</main></div>
}
function Info({label,value}:{label:string;value:string}){return <div className="rounded-2xl border border-white/[0.06] bg-[#11131B] p-4"><p className="text-[9px] uppercase tracking-wide text-[#606475]">{label}</p><p className="mt-2 break-words text-xs font-medium text-[#D1D3DC]">{value}</p></div>}
function Field({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label className="block"><span className="mb-1 block text-[10px] text-[#777B8D]">{label}</span><input type="password" value={value} onChange={e=>onChange(e.target.value)} className="h-11 w-full rounded-xl border border-white/[0.08] bg-[#181A23] px-3 text-xs outline-none focus:border-blue-500/40"/></label>}
