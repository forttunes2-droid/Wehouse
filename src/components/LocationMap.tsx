type Props={latitude:number;longitude:number;label?:string;height?:number;approximate?:boolean};
export default function LocationMap({latitude,longitude,label='Property location',height=220,approximate=false}:Props){const delta=approximate?0.015:0.006;const left=longitude-delta,right=longitude+delta,bottom=latitude-delta,top=latitude+delta;const src=`https://www.openstreetmap.org/export/embed.html?bbox=${left}%2C${bottom}%2C${right}%2C${top}&layer=mapnik&marker=${latitude}%2C${longitude}`;return <div className="overflow-hidden rounded-2xl border border-white/[0.08] bg-[#10131B]"><iframe title={label} src={src} style={{height}} className="w-full border-0" loading="lazy"/><div className="flex items-center justify-between gap-3 px-3 py-2"><div><p className="text-[10px] font-semibold text-white">{label}</p><p className="text-[9px] text-[#6D7182]">{approximate?'Approximate public area':'Verified coordinates'} · {latitude.toFixed(5)}, {longitude.toFixed(5)}</p></div><a href={`https://www.openstreetmap.org/?mlat=${latitude}&mlon=${longitude}#map=17/${latitude}/${longitude}`} target="_blank" rel="noreferrer" className="shrink-0 rounded-lg border border-white/[0.08] px-3 py-2 text-[9px] font-semibold text-violet-300">Open map</a></div></div>}

export function distanceKm(aLat:number,aLon:number,bLat:number,bLon:number){const r=6371;const rad=(v:number)=>v*Math.PI/180;const dLat=rad(bLat-aLat),dLon=rad(bLon-aLon);const x=Math.sin(dLat/2)**2+Math.cos(rad(aLat))*Math.cos(rad(bLat))*Math.sin(dLon/2)**2;return r*2*Math.atan2(Math.sqrt(x),Math.sqrt(1-x));}
