import { useEffect,useState } from 'react';
import { toast } from 'sonner';
import { supabase } from '@/lib/supabase';
type View='payments'|'payouts'|'ledger';
export default function StaffFinanceRecords({view}:{view:View}){const[rows,setRows]=useState<any[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{void(async()=>{const{data,error}=await supabase.rpc('get_my_staff_finance_queue');if(error)toast.error(error.message);const result=data||{};setRows(view==='payments'?(result.payments||[]):view==='payouts'?[...(result.withdrawals||[]).map((r:any)=>({...r,_kind:'Withdrawal'})),...(result.refunds||[]).map((r:any)=>({...r,_kind:'Refund'}))]:[...(result.commissions||[]).map((r:any)=>({...r,_kind:'Commission'})),...(result.escrow||[]).map((r:any)=>({...r,_kind:'Escrow'})),...(result.audit||[]).map((r:any)=>({...r,_kind:'Audit'}))]);setLoading(false)})()},[view]);if(loading)return <Empty text="Loading finance records…"/>;return <div className="space-y-4"><div><h2 className="text-lg font-bold">{view==='payments'?'Payments':view==='payouts'?'Payouts':'Ledger & Audit'}</h2><p className="mt-1 text-[10px] text-[#707687]">{view==='payments'?'Verified incoming payment records.':view==='payouts'?'Withdrawal and refund records.':'Commission, escrow and audit trail.'}</p></div>{rows.length===0?<Empty text="No finance records in this view."/>:<div className="space-y-2">{rows.map((row,index)=><article key={`${row._kind||view}-${row.id||index}`} className="rounded-2xl border border-white/[.06] bg-[#10141D] p-4"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="truncate text-xs font-semibold">{row._kind||row.purpose||row.booking_type||'Finance record'}</p><p className="mt-1 break-words text-[9px] text-[#666D7E]">{row.description||row.refund_reason||row.paystack_reference||row.payment_reference||time(row.created_at)}</p></div><div className="shrink-0 text-right">{money(row)&&<p className="text-xs font-bold">{money(row)}</p>}<p className="mt-1 text-[8px] uppercase text-[#6C7282]">{row.status||row.status_after||'recorded'}</p></div></div></article>)}</div>}</div>}
function money(row:any){const value=row.commission_amount??row.amount_total??row.amount??row.amount_payee;return value==null?'':`₦${Number(value).toLocaleString('en-NG',{maximumFractionDigits:2})}`}
function time(value:any){return value?new Date(value).toLocaleString():'Recorded'}
function Empty({text}:{text:string}){return <div className="rounded-2xl border border-dashed border-white/[.08] px-5 py-12 text-center text-[10px] text-[#666D7E]">{text}</div>}
