import { useEffect, useState } from 'react';
import WeHouseSelect from './WeHouseSelect';
import { createPortal } from 'react-dom';
import { toast } from 'sonner';
import { supabase, createHotelRoom, getHotelRooms } from '@/lib/supabase';
import { uploadListingImage } from '@/lib/supabase/listings';
import LocationMap from './LocationMap';
import ManageListing from './ManageListing';
import type { Profile } from '@/types';
type Stage = 'all'|'access_required'|'access_review'|'inspection_ready'|'inspection'|'visit_reviewed'|'listing_prepared'|'live'|'changes_requested'|'rejected';
const STAGES: [
    Stage,
    string
][] = [['all','All properties'],['access_required','Access evidence needed'],['access_review','Access review'],['inspection_ready','Ready for Field Officer'],['inspection','Field inspection'],['visit_reviewed','Visit reviewed'],['listing_prepared','Listing prepared'],['live','Public'],['changes_requested','Changes requested'],['rejected','Rejected']];
export default function PropertyPipelineWorkspace({ profile }: {
    profile: Profile;
}) { const visible = STAGES; const [stage, setStage] = useState<Stage>('all'); const [rows, setRows] = useState<any[]>([]), [loading, setLoading] = useState(true), [selected, setSelected] = useState<any | null>(null); async function load(quiet = false) { if (!quiet)
    setLoading(true); const { data, error } = await supabase.rpc('get_my_property_pipeline_v2', { p_stage: 'all' }); if (error) {
    if (!quiet)
        toast.error(error.message);
}
else
    setRows(Array.isArray(data) ? data : []); if (!quiet)
    setLoading(false); } useEffect(() => { void load(); const timer = window.setInterval(() => { if (document.visibilityState === 'visible')
    void load(true); }, 15000); const focus = () => void load(true); const visibility = () => { if (document.visibilityState === 'visible')
    void load(true); }; window.addEventListener('focus', focus); document.addEventListener('visibilitychange', visibility); return () => { window.clearInterval(timer); window.removeEventListener('focus', focus); document.removeEventListener('visibilitychange', visibility); }; }, [stage, profile.user_id]); if (selected)
    return <Case profile={profile} row={selected} back={() => { setSelected(null); void load(); }}/>; const shown=stage==='all'?rows:rows.filter(row=>String(row.lifecycle_stage||'access_required')===stage); return <div className="space-y-5"><div className="flex items-start justify-between gap-3"><div><h3 className="text-base font-bold">Property publication control</h3><p className="mt-1 text-[10px] text-[#707386]">One record follows access review, the independent field visit, listing preparation and publication.</p></div><button onClick={() => void load()} disabled={loading} className="shrink-0 rounded-xl border border-white/[0.08] bg-white/[0.025] px-3 py-2 text-[9px] font-semibold text-[#8D92A2] disabled:opacity-40">Refresh</button></div><div className="flex items-center justify-between gap-3 border-y border-white/[0.07] py-3"><span className="text-[9px] font-semibold uppercase tracking-wide text-[#686F80]">Lifecycle filter</span><WeHouseSelect value={stage} options={visible.map(([value,label])=>({value,label}))} onChange={setStage} eyebrow="Property publication" title="Choose a lifecycle status" ariaLabel="Filter property lifecycle queue" className="max-w-[13rem] rounded-full" /></div>{loading ? <Loading /> : shown.length === 0 ? <Empty text="No properties match this status."/> : <div className="divide-y divide-white/[0.065] border-y border-white/[0.065]">{shown.map(r => <button key={r.id} onClick={() => setSelected(r)} className="flex min-h-20 w-full items-center gap-3 px-1 py-3 text-left active:bg-white/[0.025]"><div className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-violet-500/[0.08] text-[10px] font-bold text-violet-300">{String(r.property_type || 'P').charAt(0).toUpperCase()}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-semibold">{r.property_address || r.request_code}</p><p className="mt-1 truncate text-[9px] text-[#6D7182]">{r.property_city}, {r.property_state} · {String(r.property_type || 'property').replace(/_/g, ' ')}</p><p className="mt-1 truncate text-[8px] text-[#555B6C]">{r.owner_name || r.owner_email || 'Property Partner'} · {r.request_code}</p></div><div className="flex shrink-0 items-center gap-2"><Badge value={String(r.lifecycle_stage||'access_required')}/><span className="text-[#5E6475]">›</span></div></button>)}</div>}</div>; }
function Case({ profile, row, back }: {
    profile: Profile;
    row: any;
    back: () => void;
}) { const oversight = ['admin', 'creator'].includes(profile.role); const operationsAccess = profile.role==='staff'||oversight; const stage=String(row.lifecycle_stage||'access_required'); const showAccessEvidence=operationsAccess&&(['access_review','inspection_ready'].includes(stage)||oversight&&['inspection','visit_reviewed','listing_prepared','live'].includes(stage)); return createPortal(<div className="fixed inset-0 z-[100020] bg-[#080A0F] text-white" role="dialog" aria-modal="true" aria-label="Property workflow"><main className="absolute inset-0 overflow-y-auto bg-[#0A0D14]"><header className="sticky top-0 z-10 flex min-h-14 items-center gap-3 border-b border-white/[.07] bg-[#0A0D14]/95 px-4 backdrop-blur-xl"><button onClick={back} className="grid h-10 w-10 shrink-0 place-items-center rounded-full text-xl text-[#A0A5B4] active:bg-white/[.05]" aria-label="Close property">‹</button><div className="min-w-0"><p className="truncate text-sm font-semibold">{row.property_address || 'Property workflow'}</p><p className="truncate text-[8px] text-[#666D7E]">{row.request_code} · {stage==='live'?'Published property record':'Review and action'}</p></div></header><div className="mx-auto max-w-5xl space-y-4 p-4 pb-[max(2rem,env(safe-area-inset-bottom))]"><WorkflowProgress row={row}/><SubmittedMedia row={row}/>{showAccessEvidence&&<AccessReview profile={profile} row={row} done={back}/>} {['visit_reviewed','listing_prepared','live'].includes(stage)&&<EvidenceReview inspectionId={row.id}/>}<section className="border-y border-white/[0.065] py-4"><div className="flex flex-wrap justify-between gap-3"><div><h3 className="text-base font-bold">{row.property_address}</h3><p className="mt-1 text-[10px] text-[#6D7182]">{row.property_city}, {row.property_state} · {row.request_code}</p><p className="mt-1 text-[10px] text-[#6D7182]">Property Partner: {row.owner_name || row.owner_email} {row.owner_phone ? `· ${row.owner_phone}` : ''}</p></div><Badge value={stage}/></div><div className="mt-4 grid grid-cols-2 gap-2 sm:grid-cols-4"><Info label="Type" value={row.property_type || '—'}/><Info label="Expected rent" value={row.expected_rent ? money(row.expected_rent) : '—'}/><Info label="Bedrooms" value={row.bedrooms ?? '—'}/><Info label="Bathrooms" value={row.bathrooms ?? '—'}/></div>{row.description && <p className="mt-4 rounded-xl bg-white/[0.025] p-3 text-[10px] leading-relaxed text-[#9699A8]">{row.description}</p>}</section>{row.gps_latitude != null && row.gps_longitude != null && <LocationMap latitude={Number(row.gps_latitude)} longitude={Number(row.gps_longitude)} label="Property inspection location"/>}{stage==='access_required'&&<Empty text="Access evidence is missing. This submission cannot be assigned or prepared."/>}{stage==='inspection_ready'&&operationsAccess&&<Assign row={row} done={back}/>} {stage==='inspection'&&<div className="rounded-2xl border border-violet-500/10 bg-violet-500/[0.04] p-4"><p className="text-xs font-semibold text-violet-300">Inspection in progress</p><p className="mt-1 text-[10px] text-[#7A8091]">{row.field_officer_name || 'The assigned Field Officer'} handles the independent visit only. Completion moves this property to visit review.</p></div>}{stage==='visit_reviewed'&&<Prepare row={row} done={back}/>} {stage==='listing_prepared'&&<Prepared row={row} authority={oversight} done={back}/>} {stage==='live'&&<div className="rounded-2xl border border-emerald-500/15 bg-emerald-500/[0.04] p-4"><p className="text-xs font-semibold text-emerald-300">Public listing</p><p className="mt-1 text-[10px] text-[#7A8091]">Published only after access approval, an independent Field Officer visit, visit review and final listing preparation. The private evidence remains an internal audit record and is never public media.</p></div>}{profile.role==='creator'&&!['live','rejected'].includes(stage)&&<SubmissionDecision row={row} done={back}/>}{oversight&&(row.draft_listing_id||row.listing?.id)&&<ManageListing listingId={String(row.draft_listing_id||row.listing?.id)} done={back}/>}</div></main></div>, document.body); }

function WorkflowProgress({row}:{row:any}) {
    const stage=String(row.lifecycle_stage||'access_required');
    const current = stage==='rejected'||stage==='changes_requested' ? -1 : stage==='live' ? 4 : stage==='listing_prepared' ? 3 : stage==='visit_reviewed' ? 2 : stage==='inspection' ? 1 : 0;
    const steps = ['Access review','Field visit','Visit reviewed','Listing prepared','Public'];
    return <section className="rounded-2xl border border-white/[.07] bg-white/[.02] p-4"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-semibold">Publication journey</p><p className="mt-1 text-[9px] text-[#747A8B]">One controlled path. No stage can silently make this property public.</p></div><span className={`rounded-full px-2 py-1 text-[8px] font-semibold ${current < 0 ? 'bg-red-500/10 text-red-300' : 'bg-violet-500/10 text-violet-300'}`}>{current < 0 ? 'Stopped' : `${Math.min(current + 1, 5)} of 5`}</span></div><ol className="mt-4 grid grid-cols-5 gap-1">{steps.map((step,index)=><li key={step} className="min-w-0"><div className={`h-1 rounded-full ${current >= index ? (current === 4 ? 'bg-emerald-400' : 'bg-violet-400') : 'bg-white/[.08]'}`}/><p className={`mt-2 break-words text-[7px] leading-tight sm:text-[9px] ${current >= index ? 'text-[#D7D9E2]' : 'text-[#565C6D]'}`}>{step}</p></li>)}</ol></section>;
}
function AccessReview({profile,row,done}:{profile:Profile;row:any;done:()=>void}) {
    const[data,setData]=useState<any|null>(null),[url,setUrl]=useState<string|null>(null),[loadError,setLoadError]=useState<string|null>(null),[busy,setBusy]=useState(false),[loading,setLoading]=useState(true);
    useEffect(()=>{let active=true;void(async()=>{setLoadError(null);const review=await supabase.rpc('get_property_access_review_details',{p_request_id:row.id});if(!active)return;if(review.error){setLoadError(review.error.message);setLoading(false);return}setData(review.data);const path=review.data?.video_path as string|undefined;if(!path){setLoadError('The submission record does not contain an access-video reference.');setLoading(false);return}const signed=await supabase.storage.from('property-access-private').createSignedUrl(path,900);if(!active)return;if(signed.error||!signed.data?.signedUrl)setLoadError(signed.error?.message||'WeHouse could not open the private access video.');else setUrl(signed.data.signedUrl);setLoading(false)})();return()=>{active=false}},[row.id]);
    async function decide(decision:'accept'|'reject'){let note:string|null=null;if(decision==='reject'){note=window.prompt('Explain exactly what the Property Partner must record again.');if(!note?.trim())return}if(!window.confirm(decision==='accept'?'Accept this private access recording? A Field Officer can then be assigned.':'Request new access evidence from this Property Partner?'))return;setBusy(true);const{error}=await supabase.rpc('review_property_access_evidence',{p_request_id:row.id,p_decision:decision,p_note:note});setBusy(false);if(error)return toast.error(error.message);toast.success(decision==='accept'?'Access evidence approved':'New access evidence requested');done()}
    if(loading)return <Loading/>;
    const reviewing=row.lifecycle_stage==='access_review', approved=row.lifecycle_stage!=='access_review';
    return <section className="rounded-2xl border border-violet-500/15 bg-violet-500/[.04] p-4"><p className="text-[9px] font-bold uppercase tracking-[.16em] text-violet-300">Private partner access evidence</p><h3 className="mt-2 text-sm font-semibold">{reviewing?'Property Operations review required':'Access evidence record'}</h3><p className="mt-1 text-[10px] text-[#73798A]">{reviewing?<>Confirm that code <span className="font-bold text-white">{data?.code||'unavailable'}</span> appears at the entrance and the recording continues inside.</>:<>This recording was retained for Admin and Creator oversight. The expired one-use code and recording controls are not shown after review.</>} This is separate from the Field Officer inspection.</p>{url?<video src={url} controls playsInline preload="metadata" className="mt-4 aspect-video w-full rounded-xl bg-black object-contain"/>:<p className="mt-4 rounded-xl bg-amber-500/[.05] p-3 text-[10px] text-amber-200">{loadError||'The private recording could not be opened.'}</p>}<div className="mt-3 flex gap-2">{reviewing&&<><button disabled={busy||!url} onClick={()=>void decide('reject')} className="h-11 flex-1 rounded-xl border border-amber-500/20 text-[10px] font-semibold text-amber-200 disabled:opacity-40">Request correction</button><button disabled={busy||!url} onClick={()=>void decide('accept')} className="h-11 flex-1 rounded-xl bg-violet-500 text-[10px] font-semibold disabled:opacity-40">Approve evidence</button></>}{approved&&row.lifecycle_stage==='inspection_ready'&&['admin','creator'].includes(profile.role)&&<button disabled={busy} onClick={()=>void decide('reject')} className="h-11 w-full rounded-xl border border-amber-500/20 text-[10px] font-semibold text-amber-200 disabled:opacity-40">Override decision · Request correction</button>}</div></section>;
}
function Assign({ row, done }: { row: any; done: () => void }) {
    const [officers, setOfficers] = useState<any[]>([]), [pick, setPick] = useState(''), [date, setDate] = useState(''), [loading, setLoading] = useState(true), [saving, setSaving] = useState(false);
    useEffect(() => { void (async () => {
        const { data, error } = await supabase.rpc('admin_get_field_officers_for_inspection', { p_inspection_id: row.id });
        if (error) toast.error(error.message);
        setOfficers(Array.isArray(data) ? data : []);
        setLoading(false);
    })(); }, [row.id]);
    async function assign() {
        if (!pick) return toast.error('Choose a Field Officer');
        setSaving(true);
        const { error } = await supabase.rpc('admin_assign_field_officer', { p_inspection_id: row.id, p_field_officer_id: pick, p_scheduled_date: date || null });
        setSaving(false);
        if (error) return toast.error(error.message);
        toast.success('Field Officer assigned');
        done();
    }
    return <div className="rounded-2xl border border-violet-500/15 bg-violet-500/[0.04] p-4"><h3 className="text-sm font-semibold">Assign independent inspection</h3><p className="mt-1 text-[10px] text-[#727789]">The partner access recording is approved. Choose a Field Officer in this property area.</p>{loading ? <Loading /> : officers.length === 0 ? <Empty text="No Field Officer is currently assigned to this LGA. Operations must ask the branch Admin or Creator to add capacity."/> : <div className="mt-4 space-y-2">{officers.map(o => <button key={o.user_id} onClick={() => setPick(o.user_id)} className={`w-full rounded-xl border p-3 text-left ${pick === o.user_id ? 'border-violet-500 bg-violet-500/10' : 'border-white/[0.06] bg-[#11151E]'}`}><div className="flex justify-between gap-3"><div><p className="text-xs font-semibold">{o.name}</p><p className="mt-1 text-[9px] text-[#686D7F]">{o.assigned_lga}, {o.assigned_state} · {o.active_inspections} active</p></div><p className="text-[9px] font-semibold text-violet-300">{o.distance_km != null ? `${o.distance_km} km` : 'Distance unavailable'}</p></div></button>)}<input type="date" value={date} onChange={e => setDate(e.target.value)} className="h-11 w-full rounded-xl border border-white/[0.08] bg-[#151923] px-3 text-xs"/><button disabled={saving||!pick} onClick={() => void assign()} className="w-full rounded-xl bg-violet-500 px-4 py-3 text-xs font-semibold disabled:opacity-50">{saving ? 'Assigning…' : 'Assign Field Officer'}</button></div>}</div>;
}
function Prepare({ row, done }: {
    row: any;
    done: () => void;
}) {
    const [title, setTitle] = useState(row.property_type === 'hotel' ? (row.hotel_program?.name || '') : `${String(row.property_type || 'Property').replace(/_/g, ' ')} in ${row.property_city}`), [description, setDescription] = useState(row.description || ''), [price, setPrice] = useState(String(row.expected_rent || '')), [files, setFiles] = useState<File[]>([]), [excluded] = useState<Set<string>>(new Set()), [preview, setPreview] = useState(false), [saving, setSaving] = useState(false);
    async function images() { const prepared: string[] = []; for (const file of files) {
        const uploaded = await uploadListingImage(file, `inspection-${row.id}`);
        if (uploaded.error || !uploaded.url)
            throw new Error(uploaded.error?.message || 'Image upload failed');
        prepared.push(uploaded.url);
    } return [...prepared, ...(row.photo_urls || []).filter((url: string) => !excluded.has(url))]; }
    async function save() { if (!title.trim())
        return toast.error('Public name/title is required'); if (row.property_type !== 'hotel' && !Number(price))
        return toast.error('A valid rent is required'); setSaving(true); try {
        const uploaded = await images();
        if (row.property_type === 'hotel') {
            const { error } = await supabase.rpc('admin_prepare_hotel_from_submission_v2', { p_inspection_id: row.id, p_name: title.trim(), p_description: description.trim() || null, p_images: uploaded });
            if (error)
                throw error;
        }
        else {
            const { error } = await supabase.rpc('post_property_from_inspection', { p_data: { inspection_id: row.id, title: title.trim(), description: description.trim() || null, price: Number(price), images: uploaded, videos: row.video_urls || [], property_type: row.property_type, sub_type: row.sub_type, security_deposit_amount: row.security_deposit_amount, amenities: row.amenities || [], bedrooms: row.bedrooms, bathrooms: row.bathrooms } });
            if (error)
                throw error;
        }
        toast.success('Draft prepared. It is not public yet.');
        done();
    }
    catch (error: any) {
        toast.error(error.message || 'Unable to prepare property');
    }
    finally {
        setSaving(false);
    } }
    const photos = [...(row.photo_urls || []).filter((url: string) => !excluded.has(url)), ...files.map(file => URL.createObjectURL(file))];
    if (preview)
        return <section className="space-y-4"><button onClick={() => setPreview(false)} className="text-[10px] font-semibold text-violet-300">← Continue editing</button><div className="overflow-hidden rounded-[24px] bg-[#10131A]"><div className="aspect-[16/10] bg-[#171B24]">{photos[0] ? <img src={photos[0]} alt="Listing preview" className="h-full w-full object-cover"/> : <div className="grid h-full place-items-center text-[10px] text-[#687083]">No property photo yet</div>}</div><div className="p-4"><p className="text-base font-bold">{title || 'Listing title'}</p><p className="mt-1 text-sm font-semibold text-violet-300">{row.property_type === 'hotel' ? 'Hotel' : money(price)}{row.property_type === 'hotel' ? '' : ' / year'}</p><p className="mt-3 whitespace-pre-wrap text-[10px] leading-5 text-[#8A90A0]">{description || 'No public description yet.'}</p><div className="mt-3 flex gap-2 text-[9px] text-[#777D8E]"><span>{row.bedrooms || 0} bedrooms</span><span>·</span><span>{row.bathrooms || 0} bathrooms</span></div></div></div><p className="text-center text-[9px] text-amber-300">Preview only · nothing is public</p><button disabled={saving} onClick={() => void save()} className="h-12 w-full rounded-xl bg-violet-500 text-xs font-semibold">{saving ? 'Preparing…' : 'Confirm and prepare draft'}</button></section>;
    return <section className="space-y-4 border-y border-white/[.06] py-4"><div><h3 className="text-sm font-semibold">Prepare public listing</h3><p className="mt-1 text-[9px] text-[#777C8E]">Review exactly what users will see before creating the draft.</p></div>{row.property_type === 'hotel' && <HotelProgramSummary program={row.hotel_program}/>}<input value={title} onChange={e => setTitle(e.target.value)} placeholder={row.property_type === 'hotel' ? 'Hotel name' : 'Listing title'} className="h-11 w-full border-b border-white/[.08] bg-transparent text-sm outline-none"/>{row.property_type !== 'hotel' && <input type="number" value={price} onChange={e => setPrice(e.target.value)} placeholder="Annual rent" className="h-11 w-full border-b border-white/[.08] bg-transparent text-sm outline-none"/>}<textarea value={description} onChange={e => setDescription(e.target.value)} rows={5} placeholder="Public description" className="w-full resize-none border-b border-white/[.08] bg-transparent py-2 text-sm outline-none"/><label className="block py-2 text-[10px] text-[#8A8E9E]">Add verified images<input type="file" accept="image/*" multiple onChange={e => setFiles(Array.from(e.target.files || []).slice(0, 12))} className="mt-2 block w-full text-[10px]"/></label>{files.length > 0 && <div className="grid grid-cols-4 gap-1">{files.map(file => <img key={file.name} src={URL.createObjectURL(file)} alt="Selected" className="aspect-square w-full rounded-lg object-cover"/>)}</div>}<button onClick={() => setPreview(true)} className="h-12 w-full rounded-xl bg-violet-500 text-xs font-semibold">Preview before preparing</button></section>;
}
function HotelProgramSummary({ program }: { program: any }) {
    const rooms = Array.isArray(program?.room_types) ? program.room_types : [];
    return <div className="rounded-2xl border border-violet-500/15 bg-violet-500/[.04] p-3"><p className="text-[10px] font-semibold text-violet-200">Submitted hotel programme</p><p className="mt-1 text-[9px] text-[#7D8393]">{rooms.length} room type(s) · {(program?.amenities || []).length} hotel amenity item(s). Preparing creates these room types automatically.</p><div className="mt-2 divide-y divide-white/[.06]">{rooms.map((room: any, index: number) => <div key={`${room.name}-${index}`} className="flex items-center justify-between gap-3 py-2 text-[9px]"><span className="min-w-0 truncate">{room.name} · {room.bed_type || 'Bed not specified'} · {room.guest_capacity || 2} guests</span><span className="shrink-0 font-semibold text-violet-300">{money(room.nightly_rate)} × {room.inventory || 1}</span></div>)}</div></div>;
}
function Prepared({ row, authority, done }: {
    row: any;
    authority: boolean;
    done: () => void;
}) { if (row.property_type === 'hotel')
    return <HotelDraft row={row} authority={authority} done={done}/>; const listingId=row.listing?.id||row.draft_listing_id; return <div className="rounded-2xl border border-violet-500/15 bg-violet-500/[0.04] p-4"><h3 className="text-sm font-semibold">Listing prepared — not public</h3><p className="mt-1 text-[10px] text-[#777C8E]">{row.listing?.title || 'Prepared listing'} · {money(row.listing?.price || 0)} · {(row.listing?.images || []).length} image(s)</p>{authority ? <button disabled={!listingId} onClick={async () => { if(!listingId||!window.confirm('Publish this property now? It will immediately become visible and bookable to users.'))return; const { error } = await supabase.rpc('admin_publish_inspected_listing', { p_listing_id: listingId }); if (error)
    return toast.error(error.message); toast.success('Property is now public'); done(); }} className="mt-3 w-full rounded-xl bg-emerald-500 px-4 py-3 text-xs font-semibold disabled:opacity-40">Final review passed · Publish now</button> : <p className="mt-3 rounded-xl bg-white/[0.03] p-3 text-[10px] text-[#8A8E9E]">Awaiting final Admin/Creator review. It is not visible to users.</p>}</div>; }
function HotelDraft({ row, authority, done }: {
    row: any;
    authority: boolean;
    done: () => void;
}) { const id = Number(row.hotel?.hotel_id || row.draft_hotel_id); const [rooms, setRooms] = useState<any[]>([]), [form, setForm] = useState({ room_type: '', price: '', max_guests: '2', total_rooms: '1' }); async function load() { const r = await getHotelRooms(id); if (r.error)
    toast.error(r.error.message); setRooms(r.rooms || []); } useEffect(() => { if (id)
    void load(); }, [id]); async function add() { if (!form.room_type.trim() || !Number(form.price))
    return toast.error('Room type and price are required'); const { error } = await createHotelRoom({ hotel_id: id, room_type: form.room_type.trim(), description: null, price_per_night: Number(form.price), max_guests: Number(form.max_guests) || 2, bed_type: null, images: [], amenities: [], total_rooms: Number(form.total_rooms) || 1 }); if (error)
    return toast.error(error.message); setForm({ room_type: '', price: '', max_guests: '2', total_rooms: '1' }); toast.success('Room type added'); void load(); } return <div className="rounded-2xl border border-violet-500/15 bg-violet-500/[0.04] p-4"><h3 className="text-sm font-semibold">Hotel setup — not public</h3><p className="mt-1 text-[10px] text-[#777C8E]">{row.hotel?.name || 'Hotel draft'} · {rooms.length} room type(s)</p><div className="mt-3 grid gap-2 sm:grid-cols-2"><input value={form.room_type} onChange={e => setForm({ ...form, room_type: e.target.value })} placeholder="Room type" className="h-10 rounded-xl border border-white/[0.08] bg-[#151923] px-3 text-xs"/><input type="number" value={form.price} onChange={e => setForm({ ...form, price: e.target.value })} placeholder="Price per night" className="h-10 rounded-xl border border-white/[0.08] bg-[#151923] px-3 text-xs"/><input type="number" value={form.max_guests} onChange={e => setForm({ ...form, max_guests: e.target.value })} placeholder="Max guests" className="h-10 rounded-xl border border-white/[0.08] bg-[#151923] px-3 text-xs"/><input type="number" value={form.total_rooms} onChange={e => setForm({ ...form, total_rooms: e.target.value })} placeholder="Total rooms" className="h-10 rounded-xl border border-white/[0.08] bg-[#151923] px-3 text-xs"/></div><button onClick={() => void add()} className="mt-2 rounded-xl border border-white/[0.08] px-4 py-2.5 text-[10px] font-semibold">Add room type</button>{authority ? <button onClick={async () => { if(!window.confirm('Publish this hotel now? It will immediately become visible and bookable to users.'))return; const { error } = await supabase.rpc('admin_publish_inspected_hotel', { p_hotel_id: id }); if (error)
    return toast.error(error.message); toast.success('Hotel is now public'); done(); }} className="mt-3 w-full rounded-xl bg-emerald-500 px-4 py-3 text-xs font-semibold">Final review passed · Publish now</button> : <p className="mt-3 text-[10px] text-[#8A8E9E]">Awaiting final Admin/Creator review. It is not visible to users.</p>}</div>; }
function SubmittedMedia({ row }: {
    row: any;
}) { const [active, setActive] = useState<{
    url: string;
    video: boolean;
} | null>(null); const photos = Array.from(new Set([...(Array.isArray(row.photo_urls) ? row.photo_urls : []), ...(Array.isArray(row.listing?.images) ? row.listing.images : [])])) as string[]; const videos = Array.from(new Set([...(Array.isArray(row.video_urls) ? row.video_urls : []), ...(Array.isArray(row.listing?.videos) ? row.listing.videos : [])])) as string[]; const items = [...photos.map(url => ({ url, video: false })), ...videos.map(url => ({ url, video: true }))]; return <section className="border-b border-white/[.06] py-4"><div className="flex items-end justify-between gap-3"><div><h4 className="text-sm font-semibold">Property media</h4><p className="mt-1 text-[9px] text-[#717789]">Photos and videos submitted or prepared for this property. Tap any item to inspect the original.</p></div><span className="shrink-0 text-[9px] text-[#656B7D]">{items.length} file(s)</span></div>{items.length === 0 ? <p className="mt-3 text-[10px] text-amber-200/75">No original property media was attached.</p> : <div className="mt-3 flex gap-2 overflow-x-auto pb-1 scrollbar-hide">{items.map((item, index) => <button type="button" key={`${item.url}-${index}`} onClick={() => setActive(item)} className="relative h-28 w-36 shrink-0 overflow-hidden rounded-xl bg-black">{item.video ? <video src={item.url} muted playsInline preload="metadata" className="h-full w-full object-cover"/> : <img src={item.url} alt={`Submitted property media ${index + 1}`} className="h-full w-full object-cover"/>}{item.video && <span className="absolute inset-0 grid place-items-center text-xl">▶</span>}</button>)}</div>}{active && <div className="fixed inset-0 z-[100000] flex flex-col bg-[#050609]" role="dialog" aria-modal="true" aria-label="Submitted property media preview"><header className="flex h-14 shrink-0 items-center justify-between border-b border-white/[.07] px-4"><p className="text-sm font-semibold">Submitted media</p><button type="button" onClick={() => setActive(null)} className="grid h-10 w-10 place-items-center rounded-full bg-white/[.07]" aria-label="Close media preview">×</button></header><div className="flex min-h-0 flex-1 items-center justify-center bg-black">{active.video ? <video src={active.url} controls autoPlay playsInline className="max-h-full max-w-full"/> : <img src={active.url} alt="Full submitted property preview" className="max-h-full max-w-full object-contain"/>}</div></div>}</section>; }
function EvidenceReview({ inspectionId }: {
    inspectionId: string;
}) { const [data, setData] = useState<{
    photos: string[];
    videos: string[];
} | null>(null), [active, setActive] = useState<{
    url: string;
    video: boolean;
} | null>(null), [loading, setLoading] = useState(true); useEffect(() => { let live = true; void (async () => { const { data: result, error } = await supabase.rpc('get_inspection_media_for_review', { p_inspection_id: inspectionId }); if (!live)
    return; if (error) {
    toast.error(error.message);
    setData({ photos: [], videos: [] });
}
else
    setData({ photos: Array.isArray(result?.photos) ? result.photos : [], videos: Array.isArray(result?.videos) ? result.videos : [] }); setLoading(false); })(); return () => { live = false; }; }, [inspectionId]); if (loading)
    return <div className="border-y border-white/[.06] py-4 text-[10px] text-[#707687]">Loading inspection evidence…</div>; const items = [...(data?.photos || []).map(url => ({ url, video: false })), ...(data?.videos || []).map(url => ({ url, video: true }))]; return <section className="border-y border-white/[.06] py-4"><div className="flex items-end justify-between"><div><h4 className="text-xs font-semibold">Field evidence</h4><p className="mt-1 text-[9px] text-[#717789]">Open any original photo or video before preparing or publishing.</p></div><span className="text-[9px] text-[#656B7D]">{items.length} file(s)</span></div>{items.length === 0 ? <p className="mt-3 text-[10px] text-amber-200/75">No field media was submitted.</p> : <div className="mt-3 grid grid-cols-3 gap-1.5 sm:grid-cols-5">{items.map((item, index) => <button type="button" key={`${item.url}-${index}`} onClick={() => setActive(item)} className="relative aspect-square overflow-hidden rounded-xl bg-black">{item.video ? <video src={item.url} muted playsInline preload="metadata" className="h-full w-full object-cover"/> : <img src={item.url} alt={`Inspection evidence ${index + 1}`} className="h-full w-full object-cover"/>}{item.video && <span className="absolute inset-0 grid place-items-center text-xl">▶</span>}</button>)}</div>}{active && <div className="fixed inset-0 z-[100000] grid place-items-center bg-black/90 p-3" onClick={() => setActive(null)}><button type="button" aria-label="Close evidence preview" className="absolute right-4 top-4 grid h-10 w-10 place-items-center rounded-full bg-white/10 text-xl">×</button><div className="max-h-[90vh] max-w-5xl" onClick={e => e.stopPropagation()}>{active.video ? <video src={active.url} controls autoPlay playsInline className="max-h-[88vh] max-w-full"/> : <img src={active.url} alt="Full inspection evidence" className="max-h-[88vh] max-w-full object-contain"/>}</div></div>}</section>; }
function SubmissionDecision({ row, done }: {
    row: any;
    done: () => void;
}) { const [busy, setBusy] = useState(false); async function reject() { const reason = window.prompt('Why is WeHouse rejecting this property submission? The Property Partner will see this reason.'); if (!reason?.trim())
    return; setBusy(true); const { error } = await supabase.rpc('creator_reject_property_submission', { p_inspection_id: row.id, p_reason: reason.trim() }); setBusy(false); if (error)
    return toast.error(error.message); toast.success('Submission rejected and removed from the publication workflow'); done(); } return <section className="rounded-2xl border border-red-500/15 bg-red-500/[.035] p-4"><p className="text-xs font-semibold">Creator decision</p><p className="mt-1 text-[9px] leading-5 text-[#7E8493]">Rejecting stops this submission before publication. Any unpublished draft is removed; published or actively reserved property cannot be deleted here.</p><button type="button" disabled={busy} onClick={() => void reject()} className="mt-3 min-h-11 w-full rounded-xl border border-red-500/20 text-[10px] font-semibold text-red-300 disabled:opacity-40">{busy ? 'Removing submission…' : 'Reject and remove submission'}</button></section>; }
function Badge({ value }: {
    value: string;
}) { return <span className="h-fit rounded-full bg-white/[0.06] px-2 py-1 text-[8px] font-semibold capitalize text-[#A6A9B7]">{String(value || 'unknown').replace(/_/g, ' ')}</span>; }
function Info({ label, value }: {
    label: string;
    value: any;
}) { return <div className="border-b border-white/[.055] py-2.5"><p className="text-[8px] uppercase tracking-wide text-[#5D6274]">{label}</p><p className="mt-1 text-[11px] font-semibold">{String(value)}</p></div>; }
function Empty({ text }: {
    text: string;
}) { return <div className="rounded-2xl border border-dashed border-white/[0.08] p-9 text-center text-[10px] text-[#686D7F]">{text}</div>; }
function Loading() { return <div className="grid min-h-32 place-items-center"><div className="h-6 w-6 animate-spin rounded-full border-2 border-violet-500 border-t-transparent"/></div>; }
function money(v: any) { return `₦${Number(v || 0).toLocaleString('en-NG')}`; }
