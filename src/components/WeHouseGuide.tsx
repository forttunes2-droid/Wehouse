// The WeHouse Assistant no longer renders as a global floating overlay.
// It now has a dedicated entry inside Messages so it can never cover
// navigation, chat composers or Send controls.
type Props={profile?:unknown};
export default function WeHouseGuide(_props:Props){return null}
