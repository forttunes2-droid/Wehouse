import { useEffect,useState } from 'react';
import { supabase } from '@/lib/supabase';
import { WORKER_OCCUPATION_LABELS } from '@/types';
import type { Profile } from '@/types';

type Post={id:string;kind:'story'|'portfolio';media_type:'image'|'video';storage_path:string;caption:string|null;verified_job:boolean;expires_at:string|null;created_at:string;url?:string};
type Props={worker:Profile;onBack:()=>void;onBook:()=>void;bookingActive?:boolean;onOpenBooking?:()=>void};
export default function WorkerPublicProfile({worker,onBack,onBook,bookingActive=false,onOpenBooking}:Props){const[posts,setPosts]=useState<Post[]>([]),[reviews,setReviews]=useState<any[]>([]),[viewer,setViewer]=useState<Post|null>(null),[loading,setLoading]=useState(true);useEffect(()=>{let active=true;void(async()=>{const[{data:rows},{data:reviewRows}]=await Promise.all([supabase.from('worker_showcase_posts').select('id,kind,media_type,storage_path,caption,verified_job,expires_at,created_at').eq('worker_id',worker.user_id).order('created_at',{ascending:false}),supabase.from('reviews').select('id,rating,comment,created_at').eq('reviewee_id',worker.user_id).order('created_at',{ascending:false}).limit(12)]);const enriched=await Promise.all((rows||[]).map(async(row:any)=>{const{data}=await supabase.storage.from('worker-showcase').createSignedUrl(row.storage_path,3600);return{...row,url:data?.signedUrl||''} as Post}));if(active){setPosts(enriched);setReviews(reviewRows||[]);setLoading(false)}})();return()=>{active=false}},[worker.user_id]);const occupation=WORKER_OCCUPATION_LABELS[worker.worker_occupation||'']||worker.worker_occupation||'Service professional',skills=(worker.worker_skills as string[])||[],stories=posts.filter(p=>p.kind==='story'&&(!p.expires_at||new Date(p.expires_at)>new Date())),portfolio=posts.filter(p=>p.kind==='portfolio'),rating=Number(worker.rating||0)||0,reviewCount=Number(worker.review_count||reviews.length)||reviews.length;return <div className="min-h-[100dvh] bg-[#080A0F] pb-24 text-white"><header className="sticky top-0 z-30 border-b border-white/[.06] bg-[#080A0F]/95 px-4 py-3 backdrop-blur-xl"><div className="mx-auto flex max-w-4xl items-center gap-3"><button onClick={onBack} className="grid h-10 w-10 place-items-center rounded-xl border border-white/[.07] bg-white/[.03]">←</button><div className="min-w-0 flex-1"><p className="text-[9px] font-bold tracking-[.18em] text-cyan-300">VERIFIED PROFESSIONAL</p><p className="truncate text-sm font-semibold">{worker.full_name||worker.username||'Professional'}</p></div><span className="rounded-full border border-emerald-500/20 bg-emerald-500/[.08] px-2.5 py-1 text-[8px] font-bold text-emerald-300">VERIFIED</span></div></header><main className="mx-auto max-w-4xl space-y-5 px-4 py-5 sm:px-5"><section className="rounded-3xl border border-cyan-500/15 bg-gradient-to-br from-cyan-500/[.1] via-[#111720] to-[#0D1118] p-5"><div className="flex items-start gap-4"><div className="grid h-20 w-20 shrink-0 place-items-center overflow-hidden rounded-3xl bg-gradient-to-br from-cyan-500 to-blue-600 text-2xl font-bold">{worker.avatar_url?<img src={worker.avatar_url} alt="" className="h-full w-full object-cover"/>:(worker.full_name||worker.username||'W')[0].toUpperCase()}</div><div className="min-w-0 flex-1"><h1 className="text-xl font-bold">{worker.full_name||worker.username||'Professional'}</h1><p className="mt-1 text-xs text-[#A5ABB8]">{occupation}</p><p className="mt-1 text-[10px] text-[#72798A]">{[worker.city||worker.local_government,worker.state].filter(Boolean).join(', ')||'Location not shown'}</p><div className="mt-3 flex flex-wrap items-center gap-2">{rating>0&&<span className="rounded-full bg-amber-500/10 px-2.5 py-1 text-[9px] font-semibold text-amber-300">★ {rating.toFixed(1)} · {reviewCount} review{reviewCount===1?'':'s'}</span>}{worker.worker_price&&<span className="rounded-full bg-emerald-500/10 px-2.5 py-1 text-[9px] font-semibold text-emerald-300">From ₦{Number(worker.worker_price).toLocaleString()}</span>}</div></div></div>{worker.worker_bio&&<p className="mt-4 text-xs leading-relaxed text-[#858C9B]">{worker.worker_bio}</p>}{skills.length>0&&<div className="mt-4 flex flex-wrap gap-2">{skills.map(skill=><span key={skill} className="rounded-full border border-white/[.07] px-2.5 py-1 text-[9px] text-[#A9AEBA]">{skill}</span>)}</div>}</section>{stories.length>0&&<section><div className="mb-2"><h2 className="text-sm font-bold">Work Stories</h2><p className="mt-1 text-[9px] text-[#666D7E]">Recent work updates · available for 24 hours</p></div><div className="flex gap-3 overflow-x-auto pb-2">{stories.map(post=><button key={post.id} onClick={()=>setViewer(post)} className="w-20 shrink-0"><div className="rounded-2xl bg-gradient-to-br from-cyan-300 via-blue-500 to-indigo-500 p-[2px]"><Media post={post} className="h-24 w-full rounded-[14px] bg-[#090C12] object-cover"/></div><p className="mt-1 truncate text-[9px] text-[#878D9C]">{post.caption||'Work Story'}</p></button>)}</div></section>}<section><div className="mb-3"><h2 className="text-sm font-bold">Portfolio</h2><p className="mt-1 text-[9px] text-[#666D7E]">Permanent examples of this professional’s work</p></div>{loading?<Empty text="Loading professional work…"/>:portfolio.length===0?<Empty text="No Portfolio work has been published yet."/>:<div className="grid grid-cols-2 gap-2 sm:grid-cols-3">{portfolio.map(post=><button key={post.id} onClick={()=>setViewer(post)} className="relative overflow-hidden rounded-2xl border border-white/[.06] bg-[#0D1118] text-left"><Media post={post} className="aspect-[4/3] w-full object-cover"/>{post.verified_job&&<span className="absolute left-2 top-2 rounded-full bg-emerald-500 px-2 py-1 text-[7px] font-bold text-[#04100B]">WEHOUSE JOB ✓</span>}<p className="line-clamp-2 p-2 text-[9px] text-[#A0A5B2]">{post.caption||'Professional work'}</p></button>)}</div>}</section><section><div className="mb-3 flex items-end justify-between"><div><h2 className="text-sm font-bold">Customer reviews</h2><p className="mt-1 text-[9px] text-[#666D7E]">Feedback from completed WeHouse jobs</p></div>{rating>0&&<span className="text-sm font-bold text-amber-300">★ {rating.toFixed(1)}</span>}</div>{reviews.length===0?<Empty text="No customer reviews yet."/>:<div className="space-y-2">{reviews.map(r=><article key={r.id} className="rounded-2xl border border-white/[.06] bg-[#10141D] p-4"><p className="text-[10px] text-amber-300">{'★'.repeat(Math.max(0,Math.min(5,Number(r.rating||0))))}</p>{r.comment&&<p className="mt-2 text-[10px] leading-relaxed text-[#969CAA]">{r.comment}</p>}<p className="mt-2 text-[8px] text-[#575E6E]">{new Date(r.created_at).toLocaleDateString()}</p></article>)}</div>}</section></main><div className="fixed inset-x-0 bottom-0 z-40 border-t border-white/[.08] bg-[#090B12]/96 p-3 pb-[max(.75rem,env(safe-area-inset-bottom))] backdrop-blur-xl"><div className="mx-auto max-w-4xl"><button onClick={bookingActive?onOpenBooking:onBook} className={`h-12 w-full rounded-2xl text-xs font-semibold ${bookingActive?'border border-amber-500/20 bg-amber-500/[.07] text-amber-300':'bg-cyan-500 text-[#041014]'}`}>{bookingActive?'Open current booking':'Request this professional'}</button></div></div>{viewer&&<div className="fixed inset-0 z-[90] bg-black/95" onClick={()=>setViewer(null)}><div className="mx-auto flex h-full max-w-lg flex-col justify-center p-4" onClick={e=>e.stopPropagation()}><div className="mb-3 flex items-center justify-between"><div><p className="text-xs font-semibold">{viewer.kind==='story'?'Work Story':'Portfolio work'}</p>{viewer.verified_job&&<p className="mt-1 text-[9px] text-emerald-300">Completed through WeHouse ✓</p>}</div><button onClick={()=>setViewer(null)} className="grid h-10 w-10 place-items-center rounded-full bg-white/10">×</button></div><Media post={viewer} className="max-h-[72dvh] w-full rounded-3xl bg-black object-contain" controls/>{viewer.caption&&<p className="mt-3 rounded-2xl bg-white/[.06] p-4 text-[11px] leading-relaxed text-[#D0D3DA]">{viewer.caption}</p>}</div></div>}</div>}
function Media({post,className,controls=false}:{post:Post;className:string;controls?:boolean}){if(post.media_type==='video')return <video src={post.url} className={className} controls={controls} playsInline muted={!controls}/>;return <img src={post.url} alt="Worker work" className={className}/>}
function Empty({text}:{text:string}){return <div className="rounded-2xl border border-dashed border-white/[.08] px-5 py-8 text-center text-[10px] text-[#666D7E]">{text}</div>}
