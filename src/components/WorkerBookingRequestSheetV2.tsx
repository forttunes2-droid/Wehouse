import { useMemo,useState } from 'react';
import { toast } from 'sonner';
import { createBookingRequest } from '@/lib/supabase/worker-bookings';
import { WORKER_OCCUPATION_LABELS } from '@/types';
import type { Profile } from '@/types';

type Props={worker:Profile;profile:Profile;onClose:()=>void;onCreated:(conversationId:string,bookingId:string)=>void};
export default function WorkerBookingRequestSheetV2({worker,profile,onClose,onCreated}:Props){const[description,setDescription]=useState(''),[address,setAddress]=useState(''),[date,setDate]=useState(''),[message,setMessage]=useState(''),[submitting,setSubmitting]=useState(false);const min=useMemo(()=>{const d=new Date(Date.now()+30*60*1000);const local=new Date(d.getTime()-d.getTimezoneOffset()*60000);return local.toISOString().slice(0,16)},[]);async function submit(){if(!description.trim()||!address.trim()||!date)return toast.error('Description, service address and preferred date/time are required');if(new Date(date).getTime()<=Date.now())return toast.error('Choose a date and time in the future');setSubmitting(true);const{booking,error}=await createBookingRequest(worker.user_id,WORKER_OCCUPATION_LABELS[worker.worker_occupation||'']||worker.worker_occupation||'Service',description.trim(),address.trim(),date,message.trim());setSubmitting(false);if(error||!booking)return toast.error(error?.message||'Booking request failed');toast.success('Booking request sent');onCreated(booking.conversation_id,booking.booking_id)}return <div className="fixed inset-0 z-[70] flex items-end justify-center bg-black/70 backdrop-blur-sm sm:items-center sm:p-4" onClick={onClose}><section onClick={e=>e.stopPropagation()} className="max-h-[92dvh] w-full max-w-md overflow-y-auto rounded-t-[28px] border border-white/[.08] bg-[#11141C] p-5 sm:rounded-[28px]"><div className="flex items-start justify-between gap-3"><div><p className="text-[9px] font-bold uppercase tracking-[.16em] text-blue-400">NEW SERVICE REQUEST</p><h2 className="mt-1 text-lg font-bold">{worker.full_name||worker.username||'Professional'}</h2><p className="mt-1 text-[10px] leading-relaxed text-[#6D7282]">Your preferred date starts the conversation. You and the professional can agree the final date and price before payment.</p></div><button onClick={onClose} className="grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-white/[.04] text-[#777C8B]">×</button></div><div className="mt-5 space-y-3"><Input value={description} set={setDescription} placeholder="Describe the work needed"/><Input value={address} set={setAddress} placeholder="Service address"/><label className="block rounded-2xl border border-white/[.08] bg-[#181B24] px-4 py-3"><span className="mb-1.5 block text-[9px] font-semibold uppercase tracking-wide text-[#6D7282]">Preferred date & time</span><input type="datetime-local" min={min} value={date} onChange={e=>setDate(e.target.value)} className="w-full bg-transparent text-sm text-white outline-none [color-scheme:dark]"/></label><textarea value={message} onChange={e=>setMessage(e.target.value)} rows={3} placeholder="Anything else the professional should know? (optional)" className="w-full resize-none rounded-2xl border border-white/[.08] bg-[#181B24] px-4 py-3 text-sm outline-none focus:border-blue-500/40"/><button onClick={()=>void submit()} disabled={submitting||!profile.user_id} className="h-12 w-full rounded-2xl bg-blue-500 text-sm font-semibold disabled:opacity-40">{submitting?'Sending…':'Send booking request'}</button></div></section></div>}
function Input({value,set,placeholder}:{value:string;set:(value:string)=>void;placeholder:string}){return <input value={value} onChange={e=>set(e.target.value)} placeholder={placeholder} className="h-12 w-full rounded-2xl border border-white/[.08] bg-[#181B24] px-4 text-sm outline-none focus:border-blue-500/40"/>}
