import { useEffect, useMemo, useState } from 'react';
import { supabase } from '@/lib/supabase';
import { invalidateSettingsCache } from '@/hooks/usePlatformSettings';
import { toast } from 'sonner';

interface SettingDef {key:string;label:string;description:string;type:'text'|'toggle'|'number'|'textarea'|'select';defaultValue:string;options?:{value:string;label:string}[]}
interface DomainSettingsPanelProps {title:string;description:string;settings:SettingDef[]}
const CANONICAL_KEYS:Record<string,string>={property_partner_commission_rate:'commission_apartment',hotel_commission_rate:'commission_hotel'};
const canonicalKey=(key:string)=>CANONICAL_KEYS[key]||key;

export default function DomainSettingsPanel({title,description,settings:defs}:DomainSettingsPanelProps){
 const[values,setValues]=useState<Record<string,string>>({}),[loading,setLoading]=useState(true),[saving,setSaving]=useState<Record<string,boolean>>({});
 const keys=useMemo(()=>Array.from(new Set(defs.map(d=>canonicalKey(d.key)))),[defs]);
 useEffect(()=>{let cancelled=false;void(async()=>{setLoading(true);const{data,error}=await supabase.from('platform_settings').select('key,value,is_active').in('key',keys);if(cancelled)return;if(error){toast.error('Failed to load settings');setLoading(false);return}const next:Record<string,string>={};for(const def of defs){const dbKey=canonicalKey(def.key),row=data?.find((r:any)=>r.key===dbKey&&r.is_active!==false);next[def.key]=String(row?.value??def.defaultValue)}setValues(next);setLoading(false)})();return()=>{cancelled=true}},[defs,keys]);
 function normalize(def:SettingDef,raw:string){if(def.type!=='number')return raw;if(!raw.trim())return null;const cleaned=raw.trim().replace(/,/g,'');if(!/^\d+(\.\d{0,4})?$/.test(cleaned))return null;const n=Number(cleaned);if(!Number.isFinite(n)||n<0)return null;return String(Number(n.toFixed(4)))}
 async function saveSetting(def:SettingDef,rawValue:string){const nextValue=normalize(def,rawValue);if(nextValue===null){toast.error(`${def.label} must be a valid positive number`);return}const dbKey=canonicalKey(def.key);setSaving(prev=>({...prev,[def.key]:true}));const{data:existing,error:readError}=await supabase.from('platform_settings').select('key').eq('key',dbKey).maybeSingle();if(readError){toast.error(`Could not read ${def.label}`);setSaving(prev=>({...prev,[def.key]:false}));return}let error:any=null;if(existing){({error}=await supabase.from('platform_settings').update({value:nextValue,label:def.label,description:def.description,data_type:def.type==='toggle'?'boolean':def.type==='number'?'number':'text',is_active:true,updated_at:new Date().toISOString()}).eq('key',dbKey))}else{({error}=await supabase.from('platform_settings').insert({key:dbKey,value:nextValue,label:def.label,description:def.description,category:'platform',data_type:def.type==='toggle'?'boolean':def.type==='number'?'number':'text',is_active:true,updated_at:new Date().toISOString()}))}if(error){toast.error(`Failed to save ${def.label}: ${error.message}`);setSaving(prev=>({...prev,[def.key]:false}));return}const{data:verify,error:verifyError}=await supabase.from('platform_settings').select('value').eq('key',dbKey).maybeSingle();if(verifyError||!verify||String(verify.value)!==String(nextValue)){toast.error(`${def.label} could not be verified after saving`);setSaving(prev=>({...prev,[def.key]:false}));return}setValues(prev=>({...prev,[def.key]:nextValue}));invalidateSettingsCache();toast.success(`${def.label} saved`);setSaving(prev=>({...prev,[def.key]:false}))}
 function change(def:SettingDef,value:string){setValues(prev=>({...prev,[def.key]:def.type==='number'?value.replace(/[^0-9.]/g,''):value}))}
 if(loading)return <div className="rounded-2xl border border-white/[.06] bg-[#10131B] p-5"><div className="flex justify-center py-5"><div className="h-5 w-5 animate-spin rounded-full border-2 border-violet-500 border-t-transparent"/></div></div>;
 return <section className="rounded-2xl border border-white/[.06] bg-[#10131B] p-4 sm:p-5"><div className="mb-4"><h3 className="text-sm font-semibold text-white">{title}</h3><p className="mt-1 text-[10px] leading-relaxed text-[#666A7C]">{description}</p></div><div className="divide-y divide-white/[.05]">{defs.map(def=>{const value=values[def.key]??def.defaultValue,isSaving=saving[def.key];return <div key={def.key} className="grid gap-3 py-4 first:pt-0 last:pb-0 sm:grid-cols-[minmax(0,1fr)_minmax(180px,280px)] sm:items-center"><div className="min-w-0"><p className="text-[11px] font-medium text-white">{def.label}</p><p className="mt-1 text-[10px] leading-relaxed text-[#666A7C]">{def.description}</p></div><div className="flex min-w-0 items-center justify-start gap-2 sm:justify-end">{def.type==='toggle'&&<button type="button" disabled={isSaving} onClick={()=>{const next=value==='true'?'false':'true';change(def,next);void saveSetting(def,next)}} aria-pressed={value==='true'} className={`relative h-6 w-11 shrink-0 rounded-full transition-colors disabled:opacity-50 ${value==='true'?'bg-violet-500':'border border-white/[.08] bg-[#252936]'}`}><span className={`absolute left-0.5 top-0.5 h-5 w-5 rounded-full bg-white transition-transform ${value==='true'?'translate-x-5':'translate-x-0'}`}/></button>}{def.type==='number'&&<div className="flex h-10 min-w-0 flex-1 items-center rounded-xl border border-white/[.08] bg-[#171A23] px-3 focus-within:border-violet-500/40 sm:max-w-36"><input inputMode="decimal" value={value} onChange={e=>change(def,e.target.value)} onBlur={()=>{const normalized=normalize(def,value);if(normalized!==null)setValues(prev=>({...prev,[def.key]:normalized}))}} onKeyDown={e=>{if(e.key==='Enter')void saveSetting(def,value)}} className="min-w-0 flex-1 bg-transparent text-xs text-white outline-none"/></div>}{def.type==='text'&&<input type="text" value={value} onChange={e=>change(def,e.target.value)} className="h-10 min-w-0 flex-1 rounded-xl border border-white/[.08] bg-[#171A23] px-3 text-xs text-white outline-none focus:border-violet-500/40"/>}{def.type==='select'&&def.options&&<select value={value} disabled={isSaving} onChange={e=>{const next=e.target.value;change(def,next);void saveSetting(def,next)}} className="h-10 min-w-0 flex-1 rounded-xl border border-white/[.08] bg-[#171A23] px-3 text-xs text-white outline-none focus:border-violet-500/40">{def.options.map(opt=><option key={opt.value} value={opt.value}>{opt.label}</option>)}</select>}{def.type==='textarea'&&<textarea value={value} onChange={e=>change(def,e.target.value)} rows={3} className="min-w-0 flex-1 resize-none rounded-xl border border-white/[.08] bg-[#171A23] px-3 py-2 text-xs text-white outline-none focus:border-violet-500/40"/>}{def.type!=='toggle'&&def.type!=='select'&&<button type="button" onClick={()=>void saveSetting(def,value)} disabled={isSaving} className="h-10 shrink-0 rounded-xl bg-violet-500 px-3 text-[10px] font-semibold text-white disabled:opacity-40">{isSaving?'Saving…':'Save'}</button>}</div></div>})}</div></section>;
}
