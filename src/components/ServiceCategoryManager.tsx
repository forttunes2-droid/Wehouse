import type { Profile } from '@/types';

export default function ServiceCategoryManager({ profile: _profile }: { profile: Profile }) {
  return (
    <div className="text-center py-10">
      <p className="text-sm text-[#5C5E72]">Service category management coming soon</p>
      <p className="text-[10px] text-[#3A3A4A] mt-2">Manage worker service categories and skills</p>
    </div>
  );
}
