export function RoommateChoice({title,value,options,onChange}:{title:string;value:string;options:Array<[string,string]>;onChange:(value:string)=>void}){return <div><p className="mb-2 text-[9px] font-semibold uppercase tracking-wide text-[#74798A]">{title}</p><div className="flex flex-wrap gap-2">{options.map(([key,label])=><button type="button" key={key} onClick={()=>onChange(key)} className={`min-h-10 rounded-xl px-4 text-xs ${value===key?'bg-violet-500 text-white':'border border-white/[.08] text-[#A4A8B5]'}`}>{label}</button>)}</div></div>}

export function RoommateInfo({label,value}:{label:string;value:string}){return <div className="rounded-2xl border border-white/[.06] bg-[#11131B] p-4"><p className="text-[9px] uppercase tracking-wide text-[#626778]">{label}</p><p className="mt-2 text-sm font-semibold">{value}</p></div>}
